# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository layout

This repository contains a single Shopify app workspace. All code lives under [wholesale/](wholesale/) — the repo root holds only this file and `.git`. Inside `wholesale/` are three independently-deployable pieces:

| Piece | Path | Purpose |
|---|---|---|
| **Shopify app** (server) | `wholesale/` | React Router 7 (Remix-style) Shopify embedded admin app + webhook handlers + scheduler |
| **Registration form** (embedded) | `wholesale/registration-form/` | Standalone Vite + React 19 + MUI form, built into the theme extension's `assets/` |
| **Theme extension** | `wholesale/extensions/theme-extension/` | Shopify theme app extension that exposes the registration form as a storefront block |

Two `package.json` files; treat them as separate workspaces. Most work happens in `wholesale/`.

## Primary spec — read first for non-trivial work

[wholesale/CLAUDE.md](wholesale/CLAUDE.md) is the canonical project spec. It covers the wholesale app's purpose, stack, implementation status (✅ / 🟡 / ⏳), critical project rules, and the **maintenance protocol** that requires spec updates alongside meaningful code changes.

[wholesale/INTEGRATIONS.md](wholesale/INTEGRATIONS.md) is the deep technical reference (~1,400 lines, 24 sections) for the Shopify → QBO → NMI order-to-payment pipeline. Architecture, end-to-end flow, scheduler logic, error taxonomy, env vars, collections, edge cases, deployment.

Open both before touching webhook, orchestrator, integration, or scheduler code.

## Commands

All commands run from `wholesale/` unless noted. Node 20.19+ / 22.12+ required (`engines` in `package.json`).

### Shopify app (wholesale/)

```bash
npm install                      # install deps
shopify app dev                  # local dev with auto-tunnel (preferred)
npm run dev                      # alias for above
npm run build                    # production build via react-router build
npm run start                    # serve a prebuilt bundle (react-router-serve)
npm run lint                     # ESLint over the app
npm run typecheck                # react-router typegen + tsc --noEmit
npm run deploy                   # shopify app deploy — pushes webhooks + scopes + extensions
npm run config:use <config>      # switch between shopify.app.*.toml profiles
npm run config:link              # link to a Partners app
shopify app webhook trigger \    # synthetic webhook (useful while orders/create approval pending)
  --topic=orders/create \
  --api-version=2025-07 \
  --address=https://<tunnel>/webhooks/orders/create
```

`npm run predeploy` runs automatically before `deploy` and triggers `build:theme` (see below).

### Registration form (wholesale/registration-form/)

Driven by Vite. Build output lands in the theme extension's `assets/` so the storefront block can load it.

```bash
npm install                      # from inside registration-form/
npm run dev                      # Vite dev server (port 5173)
npm run build                    # production build → theme-extension/assets/
npm run lint
npm run preview                  # serve the built bundle locally
```

Or, from `wholesale/`:

```bash
npm run build:theme              # equivalent to `npm --prefix registration-form run build`
```

### Theme extension

Deployed via `shopify app deploy` (along with the Shopify app and any other extensions).

## Architecture — the big picture

### Order-to-payment pipeline (the critical path)

The Shopify app's reason for existing: turn new Shopify orders into paid QBO invoices via NMI.

```
Shopify orders/create webhook  →  routes/webhooks.orders.create.jsx   (URL: /webhooks/orders/create, file-based)
   → services/order/order.service.js     (idempotent orchestrator)
       → services/customer/customer.service.js   (QBO find-or-create + NMI vault find-or-create)
       → services/invoice/invoice.service.js     (claim-first invoice creation, calls services/qbo/qbo.service.createInvoice)
   → scheduler tick (Agenda 5; 30s in dev, cron 15th+last in prod)
       → services/scheduler/jobs/processPendingPayments.job.js
           PASS 1: services/payment/payment.service.chargeInvoice  (NMI charge for pending invoices)
           PASS 2: services/invoice/invoice.service.propagateSuccessfulPayment  (re-sync paid invoices)
   → propagateSuccessfulPayment
       → services/qbo/qbo.service.recordPayment + services/shopify/shopify.service.markOrderPaid + DB update
```

Three idempotency layers prevent duplicate invoices:
1. **Webhook-id dedup** — `seenWebhookIds[]` on the ShopifyOrder doc catches Shopify's at-least-once retries.
2. **Atomic claim** — `findOneAndUpdate({ processingStatus: { $in: claimable } }, $set: 'processing' })`. Loser exits.
3. **Claim-first Invoice insert** — `Invoice.create({ qboInvoiceId: null, qboCreationStatus: 'claimed' })` fires the unique `(shop, shopifyOrderId)` index **before** the QBO POST. Losers `waitForClaimToComplete` (poll up to 30s). This is the structural fix for the user-reported duplicate-invoice bug — see [wholesale/INTEGRATIONS.md §13](wholesale/INTEGRATIONS.md).

<<<<<<< HEAD
### Module boundaries (project laws)

- No `process.env.X` outside service config files. Add new env keys to the right per-service config in [services/<svc>/<svc>.config.js](wholesale/app/services), using `readEnv` / `readInt` / `readBool` from [utils/env.utils.js](wholesale/app/utils/env.utils.js). Boot aggregator lives at [app/configs/index.js](wholesale/app/configs/index.js).
- No QBO calls outside `services/qbo/`. Same rule for `services/nmi/` and `services/shopify/`. Each integration is internally split: `<svc>.apis.js` owns I/O, `<svc>.service.js` exposes domain methods, GraphQL strings live in `<svc>.queries.js` / `<svc>.mutations.js`.
- API handlers in [app/api/](wholesale/app/api) are thin: validate, auth, call a service, respond. All business logic lives in `services/`.
- Models in `app/models/` are schema + indexes only.
- Errors are typed as `PermanentError` or `TransientError` ([utils/retry.utils.js](wholesale/app/utils/retry.utils.js)) so retry layers can decide. QBO + NMI clients retry transients up to `HTTP_RETRY_ATTEMPTS` (default 4); the scheduler retries NMI charges up to `PAYMENT_MAX_RETRY_ATTEMPTS` (default 6).
=======
### Wholesale registration form feature

A separate flow from the order pipeline. Storefront customers fill out a 3-step form embedded via the theme extension; approved applicants become Shopify wholesale customers.

```
Storefront Liquid block
  → registration-form/ React SPA (Vite, 3-step react-hook-form + Yup)
      Step 1 — name, email, phone, password, credentials (+ file uploads), referral
      Step 2 — billing/shipping address, tax info
      Step 3 — payment method, signature, terms
  → POST /api/registration-form  (Shopify app proxy: authenticate.public.appProxy)
      → uploads files to Shopify Files API (stagedUploadsCreate → fileCreate → poll)
      → hashes password (scrypt) and card PAN (HMAC-SHA256 keyed by SHOPIFY_API_SECRET)
      → WholesaleApplication.create() in MongoDB
      → customerCreate() via Admin GraphQL → adds "Pending" tag, sends acknowledgment email
  → Admin dashboard (app.customers._index.jsx + app.customers.$id.jsx)
      → Review → adds "Approved" tag + sends invite via customerSendInvite
      → Decline → deletes Shopify customer + MongoDB doc
```

**Key files:**

| File | Role |
|---|---|
| `registration-form/src/RegistrationForm.jsx` | Form root; `useForm` with `mode:'onTouched'`, `reValidateMode:'onChange'`; step navigation |
| `registration-form/src/schema/step*.schema.js` | Yup schemas per step; `step*Fields` arrays used for partial validation on "Continue" |
| `registration-form/src/components/SignaturePad.jsx` | Draw (canvas + toBlob) or typed signature; blob stored via `savedBlobRef` and restored on remount |
| `registration-form/src/components/Dropzone.jsx` | File picker; `has-file` state renders a green card |
| `app/api/registration-form.js` | App proxy POST handler |
| `app/utils/shopifyNoteMap.js` | Canonical map of credential IDs → Shopify customer note keys (source of truth for both form and note builder) |
| `app/utils/buildShopifyNote.js` | Assembles the note string written to the Shopify customer record |
| `app/utils/shopifyCustomer.js` | `customerCreate` + `customerSendInvite` Admin GraphQL helpers |
| `app/routes/app.customers._index.jsx` | Admin list page (search, status filter chips, Review/Revoke/Decline actions) |
| `app/routes/app.customers.$id.jsx` | Admin detail page (all fields, signature image/text, payment, credentials, license files) |

**Registration form validation gotcha** — `credentials` and `referrals` use object-level `.test('one-selected', …)` in Yup. `reValidateMode:'onChange'` only re-validates the specific path that changed (e.g. `credentials.acupuncturist.selected`), not the parent object test. After any checkbox toggle, call `trigger('credentials')` / `trigger('referrals')` explicitly.

### Module boundaries (project laws)

- No `process.env.X` outside [services/config.server.js](wholesale/app/services/config.server.js). Required values are asserted at boot.
- No QBO calls outside `services/qbo/`. Same rule for `services/nmi/` and `services/shopify/`.
- Models in `app/models/` are schema + indexes only. Business logic lives in services.
- Errors are typed as `PermanentError` or `TransientError` ([services/retry.server.js](wholesale/app/services/retry.server.js)) so retry layers can decide. QBO + NMI clients retry transients up to `HTTP_RETRY_ATTEMPTS` (default 4); the scheduler retries NMI charges up to `PAYMENT_MAX_RETRY_ATTEMPTS` (default 6).
- **CSS only in `registration-form/src/styles/registration-form.css`** — never add `style={{}}` props or CSS classes to admin routes (`app/routes/app.*.jsx`). Admin UI uses Polaris web components (`s-*` tags) styled through their own props.
- **New admin API endpoints go in `app/api/`** and are registered manually in `app/routes.js` (not file-based routes). Follow the existing pattern: `route("/api/admin/customers/:id/action", "api/admin-action.js")`.
- **After any change to `registration-form/src/`**, run `npm run build:theme` from `wholesale/` to regenerate `extensions/theme-extension/assets/react-app-bundle.*`. The storefront loads the bundle from there; changes are invisible until rebuilt.
>>>>>>> f25e7c0c25b569010264e3d301afe4440a830172

### Boot sequence ([entry.server.jsx](wholesale/app/entry.server.jsx))

1. Mongo connect.
2. `verifyCriticalIndexes` — logs `[boot] index OK` / `[boot] index MISSING` for the two unique `(shop, shopifyOrderId)` indexes. Missing means duplicate rows are blocking the index build; cleanup script in INTEGRATIONS.md §22.4.
3. `assertSafeTestCardConfig` — scrubs `NMI_TEST_*` env vars if `NMI_ENVIRONMENT !== 'sandbox'`.
4. Boot banner — prints every relevant env var (secrets masked), URLs in use, scheduler mode. Use this to confirm config when something behaves unexpectedly.
5. `getAgenda()` — coalescing singleton; starts scheduler and registers `process-pending-payments` against current env (dev interval or prod cron).

### Session storage

Note: `prisma/schema.prisma` is template residue. Active session storage is **MongoDB** via `@shopify/shopify-app-session-storage-mongodb` (configured in [app/shopify.server.js](wholesale/app/shopify.server.js)), using the same `MONGODB_URI` as the app data. The Prisma session table is unused. The `npm run setup` script (`prisma generate && prisma migrate deploy`) is also legacy from the template — running it is harmless but doesn't affect production behavior.

### Webhook registration (two paths)

- **Declarative** (preferred once approved) — `[[webhooks.subscriptions]]` blocks in the active `shopify.app.<config>.toml`. Pushed by `shopify app deploy`.
- **Programmatic** — `ensureProtectedWebhooks` from [services/shopify/shopify.service.js](wholesale/app/services/shopify/shopify.service.js) runs from `app/routes/app.jsx`'s loader on every admin page load. Idempotent. Used when `orders/create` (a protected customer data topic) is awaiting Partners-dashboard approval.

## Configuration profiles

Multiple `shopify.app.*.toml` files exist:
- [shopify.app.toml](wholesale/shopify.app.toml) — default
- [shopify.app.dev-rk.toml](wholesale/shopify.app.dev-rk.toml) — developer-specific dev profile

Switch with `npm run config:use <name>`. The active profile drives `shopify app dev` / `shopify app deploy` and determines which Partners app is targeted.

## MCP servers configured for this repo

- [wholesale/.mcp.json](wholesale/.mcp.json) and [wholesale/.cursor/mcp.json](wholesale/.cursor/mcp.json) wire up the Shopify Dev MCP (used by Claude Code, Cursor, Copilot, Gemini CLI). Use the MCP's `search_docs_chunks`, `graphql_schema`, `validate_graphql_codeblocks` etc. when writing Shopify Admin GraphQL queries or mutations.

## Gotchas worth knowing up front

These are pulled from [wholesale/README.md](wholesale/README.md) and the integration-spec; they bite in non-obvious ways:

- **Embedded-app navigation** — inside the admin iframe, use `Link` from `react-router` (not `<a>`), use the `redirect` returned from `authenticate.admin` (not `react-router`'s `redirect`), and use `useSubmit` from `react-router`.
- **NMI sandbox vs production hosts** — sandbox keys are rejected on `secure.nmi.com` and vice versa. Always match `NMI_ENVIRONMENT` to the key. Same idea for QBO.
- **QBO refresh tokens rotate on every refresh** — they're seeded once from `QBO_WHOLESALE_REFRESH_TOKEN` env, then Mongo is the source of truth (`qbo_tokens` collection). Concurrent refreshes are coalesced via an in-flight promise.
- **Webhook handler returns 200 immediately.** Downstream work is fire-and-forget. Never block the webhook response on QBO/NMI calls.
- **`orders/create` is a protected customer data topic.** It requires Partners-dashboard approval. Until then, use synthetic webhook triggers and the programmatic registration path.
- **Windows + Prisma ARM64 error** — if you hit `query_engine-windows.dll.node is not a valid Win32 application`, set `PRISMA_CLIENT_ENGINE_TYPE=binary`. (Mostly irrelevant since session storage is MongoDB now, but the Prisma client still initializes.)
<<<<<<< HEAD
=======
- **Polaris `s-button icon="…"` accepts only exact names from `privateIconArray`** — valid examples: `"check"`, `"undo"`, `"delete"`, `"arrow-left"`. `"checkmark"` is not valid and silently shows no icon. Check `node_modules/@shopify/polaris-types/dist/polaris.d.ts` line ~203 for the full list.
- **React Router 7 auto-revalidates loaders after every fetcher action** — do not call `revalidator.revalidate()` manually inside a `useEffect` that has `revalidator` in its dep array; the reference changes on each state transition and causes an infinite loop. Remove the manual call and rely on auto-revalidation.
>>>>>>> f25e7c0c25b569010264e3d301afe4440a830172

## When updating code, update the spec

The maintenance protocol in [wholesale/CLAUDE.md §6](wholesale/CLAUDE.md) requires that meaningful code changes ship with corresponding updates to `wholesale/CLAUDE.md` (status table + changelog) and `wholesale/INTEGRATIONS.md` (affected sections). This is per the project owner's explicit request. Trivial fixes (whitespace, comments) are exempt.

## Implementation status (snapshot)

This list focuses on the order-to-payment pipeline. Detailed flow lives in [wholesale/INTEGRATIONS.md](wholesale/INTEGRATIONS.md).

| Module | Status | Notes |
|---|---|---|
| Shopify orders/create webhook ingest | ✅ | `app/routes/webhooks.orders.create.jsx` → `processShopifyOrder` |
| QBO customer + invoice creation | ✅ | claim-first invoice insert (§13.4) |
| NMI vault add + sale | ✅ | vault `add_customer` happens ONCE at registration (`api/registration-form.js`); order/payment flows only read + validate the stored id (`customer.service` mirror + `validateCustomerVault` pre-flight in `chargeInvoice`). One vault per customer at `wholesale_applications.nmiCustomerVaultId` — the same vault carries either method. ACH adds a separate billing profile inside that vault; its id is stored at `wholesale_applications.payment.ach.nmi_billing_id` (NMI's billing_id, not a second vault). NMI's `customer_vault_id` is enough on its own to charge the vault's default billing; ACH-method invoices additionally pass `billing_id` to target the ACH entry. |
| Scheduler PASS 1 (auto-charge) | ✅ | card AND ACH via `paymentMethod: { $in: ['card', 'ach'] }` filter (§9.2). Cheque invoices are still held for manual settlement. `payment.service.chargeInvoice` calls `resolveInvoiceVault` to pick ids: card → `{ vaultId: nmiCustomerVaultId, billingId: null }`, ACH → `{ vaultId: nmiCustomerVaultId, billingId: nmiAchBillingId }`. The vault id is the same on both sides; the ACH billing id is the discriminator NMI uses to target the ACH billing profile inside the vault. Same retry, fee, audit, and pause semantics for both methods. **ACH approval does NOT immediately mark the invoice paid** — see PASS 1.7. |
| Scheduler PASS 1.7 (ACH settlement check) | ✅ | New CRON pass. Polls NMI's `query.php` for the current `condition` of every invoice in `paymentStatus: 'awaiting_settlement'` and either settles (apply credit, run downstream sync, → `paid`), returns (drop credit, write a `declined`/`error` PaymentAttempt, → `pending`/`failed`), or leaves the invoice alone (still in the 1–3 day ACH wait window). The settlement decision is owned by `payment.service.checkAchSettlement`. "Still pending" remarks are throttled to once per day; settle / return remarks fire on every state change. Spec: INTEGRATIONS.md §9.5.1. |
| Manual "Sync ACH status" (admin, on-demand) | ✅ | Order Details button for ACH invoices awaiting settlement. Runs the same reconciliation as the CRON for ONE invoice via shared `reconcileAchInvoice`; endpoint `POST /api/admin/orders/:id/sync-ach-status`. Atomic per-invoice lock (`achSyncInProgress`) prevents duplicate concurrent syncs (409 while in progress). Records audit trail + remark + return codes, and surfaces last-sync timestamp + latest NMI status (`achSyncLastAt/Status/Condition/Source/By`). Spec: INTEGRATIONS.md §9.5.1 (on-demand sync). |
| Scheduler PASS 2 (sync retry) | ✅ | method-agnostic |
| Admin Retry payment (card) | ✅ | `/api/admin/orders/:id/retry-payment` |
| Admin Mark cheque paid | ✅ | `/api/admin/orders/:id/mark-cheque-paid` — records `manualPayments[]`, propagates to QBO + Shopify |
| Admin Charge card fallback (cheque → card) | ✅ | `/api/admin/orders/:id/charge-card` — flips invoice method only |
| Partial payments | ✅ | Status flows `pending → partially_paid → paid` via `deriveInvoicePaymentStatus`; cheque receipts + card retries both support `amount` arg |
| Pending-approval replay | ✅ | `replayPendingOrdersForCustomer` on customer approve |
| `orders/cancelled` webhook | ✅ | `routes/webhooks.orders.cancelled.jsx` → `handleOrderCancelled`. Order doc flips to `processingStatus: 'cancelled'`, linked invoice to `paymentStatus: 'cancelled'`, QBO invoice voided when `amountPaid === 0` (skipped for paid/partially-paid invoices). CRON auto-skips via existing `paymentStatus: 'pending'` filter. |
| `fulfillments/create` + `fulfillments/update` webhooks (shipment tracking) | ✅ | `routes/webhooks.fulfillments.{create,update}.jsx` → `order.service.handleFulfillmentUpdate`. Captures carrier + tracking number + `shipment_status` + resolved carrier deep-link onto `ShopifyOrder.fulfillments[]` (current state) + `trackingHistory[]` (append-only) + `trackingUpdatedAt`; idempotent via `seenWebhookIds[]`. Carrier→URL map + resolver in pure `utils/shipping.constants.js` (UPS/FedEx/USPS/DHL + `CARRIER_TRACKING_URLS` env extras via `services/order/tracking.config.js`), Shopify `tracking_url` fallback. Displayed as a "Shipment tracking" section on Order Details (`app.orders.$id.jsx`) with `ShipmentStatusBadge` + clickable carrier links. §4.6 |
| QBO customer-facing emails | ✅ | `dispatchInvoiceLifecycleEmails` in `invoice.service.js`. Fires from `createInvoiceForOrder` (initial invoice) and `propagateSuccessfulPayment` (re-send after every payment that grew `amountPaid` or moved `paymentStatus`). Admin "Send invoice" button in Order Details → `POST /api/admin/orders/:id/send-invoice` for manual resends. Uses QBO `POST /invoice/{id}/send` only — no separate payment-receipt channel; the invoice itself carries the up-to-date balance + payments list. Idempotent via `invoiceEmailSentAt` / `invoiceEmailedStatus` / `invoiceEmailedAmountPaid` on the Invoice doc. Best-effort — failures land on `lastEmailError`, never block payment sync. |
| Email history audit ledger | ✅ | `Invoice.emailEvents[]` append-only ledger — one row per `/invoice/<id>/send` attempt (success + failure). Captures `triggerType` (auto/manual), `triggeredBy` (`'system'` or admin email), `source` (`invoice_created` / `payment_recorded` / `status_changed` / `manual_resend`), `recipient`, `status`, `errorMessage`, and snapshots of `paymentStatus` / `amountPaid` at send time. Rendered as "Email history" section on Order Details. Single shared writer `recordEmailEvent` in `invoice.service.js`. |
| NMI admin section (Dashboard / Customers / Payments / Transactions / Failed / Refunds) | ✅ | New top-level **NMI** nav item under QBO. Layout route `app/routes/app.nmi.jsx` + six tab routes. Pulls live from NMI's `query.php` (XML response, no STARTPOSITION pagination — we constrain via mandatory `start_date` / `end_date` windows, default 30 days, and paginate the parsed array client-side at 50/page). New service helpers in `services/nmi/nmi.service.js`: `parseNmiTransactions` / `parseNmiCustomerVaults` (regex-based XML block extractor — no parser dependency), `latestAction`, `toNmiDate` / `fromNmiDate` (YYYYMMDDhhmmss), `listNmiTransactions({ condition?, transactionType?, actionType?, result?, customerVaultId?, transactionId?, orderId?, invoiceId? })`, `listNmiCustomerVaults({ email?, customerVaultId? })`, `getNmiDashboardSnapshot({ periodDays })`. Dashboard composes 2 parallel NMI calls (transactions window + customer vault) and aggregates counts in JS — NMI has no SUM/COUNT GROUP BY. Per-metric failures degrade to `null` and surface as a warning banner via the `errors[]` array. Tabs share a period-chip + filter-chip pattern; the Payments tab restricts to sale/capture/credit actions, Refunds expands one row per refund action in a transaction's lifecycle (partial refunds get separate rows), Failed Payments has a "Latest action / Any action" toggle so admins can see transactions whose retry eventually succeeded after a decline. |
| QBO admin section (Dashboard / Customers / Transactions / Invoices) | ✅ | New top-level **QBO** nav item under Orders. Layout route `app/routes/app.qbo.jsx` + four tab routes: `app.qbo.dashboard.jsx`, `app.qbo.customers.jsx`, `app.qbo.transactions.jsx`, `app.qbo.invoices.jsx`. Dashboard pulls live counts (customers / invoices / paid / pending / overdue) + a per-month revenue snapshot (billed + collected) + recent payments + recent invoices in parallel; per-metric failures degrade to `null` and surface in a warning banner so one bad query doesn't take the page down. Customers/Transactions/Invoices tabs are paginated lists (50/page, QBO's STARTPOSITION pagination) with filter chips + free-text search. All data fetched live via QBO QL — no DB caching layer; "Refresh" buttons revalidate the loader. New service helpers in `services/qbo/qbo.service.js`: `listCustomers` / `countCustomers` / `listInvoices` / `countInvoices` / `listPayments` / `countPayments` / `getDashboardSnapshot`. Shared `runListQuery` + `runCountQuery` keep the QBO QL `/query` plumbing in one place. |
| Auto-charge pause / resume (per-invoice) | ✅ | Card-preferred invoices only. Order Details page exposes **Pause auto-charge** / **Resume auto-charge** buttons + a status badge ("Auto-charge active" / "Auto-charge paused") + a paused-state banner with who/when/why. Persisted as `Invoice.autoChargePaused` (+ `autoChargePausedAt`, `autoChargePausedBy`, `autoChargePauseNote`, `autoChargeResumeAt`, `autoChargeResumedBy`). CRON PASS 1 (card charge) and PASS 1.5 (cheque/ACH + failed-card reminder) both filter `autoChargePaused: { $ne: true }` so paused invoices are skipped entirely. Manual settlement (Retry payment, Charge card, Mark cheque paid) remains available while paused. Endpoints: `POST /api/admin/orders/:id/pause-auto-charge` + `POST /api/admin/orders/:id/resume-auto-charge`, both accept optional `{ note }` body that appends to the remarks ledger. Idempotent — re-pausing refreshes the timestamps, re-resuming records a "was already running" confirmation. |
| Check-payment reminder CRON | ✅ | Separate job `process-check-reminders` (prod: daily cron `0 2 * * *`, env `REMINDER_CRON`; dev/test: `REMINDER_INTERVAL`, currently every minute). Emails QBO invoice reminders on a three-stage `first` / `second` / `card` ladder for unpaid **Check** invoices only — **Day 9 / Day 11 / Day 13** in production, **Minute 1 / 3 / 4** in testing mode (`REMINDER_USE_MINUTES=true`). Notification-only — never charges; the final card-on-file charge is a manual admin action. Eligibility: `paymentMethod: 'check'` + `paymentStatus ∈ {pending, partially_paid}` + `qboCreationStatus: 'created'` + `qboInvoiceId` set + `reminderPaused ≠ true`. Paid invoices drop out automatically; **Pause/Resume auto email notifications** control on Order Details (`reminderPaused` flag, `POST /api/admin/orders/:id/pause-reminders` + `/resume-reminders`) lets an admin mute one invoice — distinct from the card `autoChargePaused`. After the final stage, a **`recurring`** reminder repeats until paid at `REMINDER_REPEAT_DAYS` (prod, default 2) / `REMINDER_REPEAT_MINUTES` (test, default 1), throttled by the most recent send. Dedup + audit via `Invoice.paymentReminders[]` (named stages sent once; `recurring` accumulates one row/cycle) plus `emailEvents[]` (`source: 'payment_reminder'`) and `remarks[]` (`kind: 'cron_payment_reminder'`). "Current level wins" ladder fires each named stage at most once, in order. Thresholds env-tunable: `REMINDER_DAY_FIRST/SECOND/CARD`, `REMINDER_MIN_FIRST/SECOND/CARD`, `REMINDER_REPEAT_DAYS/MINUTES`. `services/reminder/*` + `services/scheduler/jobs/processCheckReminders.job.js`. Spec: INTEGRATIONS.md §10.5. |
| Payment-preference change → realign open invoices | ✅ | `services/invoice/paymentPreference.service.applyPaymentPreferenceToOpenInvoices`. On a card/ACH/check preference change, realigns every unpaid/open invoice (`pending`/`failed`, `amountPaid 0`) to the new method — recomputes the per-method processing fee + due date, rewrites the QBO fee line via `qbo.service.setInvoiceProcessingFee` (sparse `Line` rewrite + `DueDate`), resets `failed → pending`, mirrors `CustomerMap.paymentMethod`, and audits via `WholesaleApplication.paymentMethodHistory[]` + per-invoice `admin_action` remark. Triggers: customer `/api/update-profile` (best-effort) + admin `POST /api/admin/customers/:id/payment-method` (UI on Customer detail page). Excludes paid/partial/in-flight. §7.3.1 |
| Per-method invoice due dates | ✅ | Invoice `DueDate` (QBO) + local `dueAt` computed at creation from the locked `paymentMethod`: cheque → `CHEQUE_DUE_DATE`, ACH → `ACH_DUE_DATE`, card → `CARD_DUE_DATE` (each days-from-order-date, falling back to `INVOICE_TERMS_DAYS`). `invoiceConfig.dueDaysByMethod` + `dueDaysForMethod()` resolver in `services/invoice/invoice.config.js`; used in `createInvoiceForOrder`. Spec: INTEGRATIONS.md §7.3. |
| Immediate Payment — self-pay link on QBO invoice | ✅ (backend) | New 4th `paymentMethod`/`customerPaymentPreference` enum value `immediate`. NOT auto-charged (PASS 1 filters card/ach; no stored vault). `createInvoiceForOrder` mints an opaque `Invoice.payToken` and bakes a `Pay online: <url>` line into the QBO `CustomerMemo` (auto-linkifies in the emailed invoice/PDF — link only, no QR). The link base is `PAY_LINK_BASE_URL` (stable; falls back to `SHOPIFY_APP_URL`); an admin **"Refresh link on invoice"** action (`POST /api/admin/orders/:id/refresh-pay-link`) re-stamps the memo if the base ever changes. Public, non-embedded page `/pay/:token` (`api/pay/pay.jsx`) renders an **NMI Collect.js** card form (iframe fields — card never hits our server), then its action recomputes the outstanding balance server-side and charges `nmi.service.chargeWithPaymentToken` (`type=sale` + `payment_token`); on approval settles via `payment/payLink.service.settleHostedPayment` → the shared `propagateSuccessfulPayment` (QBO Payment + Shopify mark-paid + order/invoice state). Settlement deduped on the NMI txn id (`Invoice.payTransactionIds[]`). Fee = `INVOICE_FEE_RATE_IMMEDIATE` (default 3%, baked at creation). Config: `NMI_COLLECT_JS_URL` + `NMI_PUBLIC_KEY` + `PAY_LINK_BASE_URL`. Order Details surfaces the (live) link + copy + refresh. **Preference is set via admin `POST /api/admin/customers/:id/payment-method` (registration-form UI for it is intentionally deferred).** Spec: INTEGRATIONS.md §9.6. |
| Drop-ship orders → unpaid QBO invoice + dedicated collection CRON | ✅ | Orders placed by `DROPSHIP_RETAIL_CUSTOMER_EMAIL` are diverted in `processShopifyOrder` (`invoiceDropshipOrder`) **immediately after the atomic claim** — skipping the approval gate + wholesale NMI-vault customer setup — to create an **UNPAID** QBO invoice (`createInvoiceForOrder({ isDropship: true })`, `paymentMethod: 'dropship'` = 0% fee + outside the wholesale charge filter; `Invoice.isDropship`), then set terminal `processingStatus: 'dropship_invoiced'`. `ensureDropshipCustomerMap` find-or-creates one QBO customer (by email) without NMI/billing requirements. A **dedicated CRON** `process-dropship-payments` (prod: monthly `DROPSHIP_PAYMENT_CRON=30 0 1 * *`; testing: `DROPSHIP_PAYMENT_INTERVAL=2 minutes`) collects each invoice by charging the single configured `DROPSHIP_NMI_VAULT_ID` via the shared `chargeInvoice` → `propagateSuccessfulPayment` (QBO recordPayment + Shopify markPaid → order `completed`); a PASS B replays downstream sync. Duplicate-safe: claim-first invoice index + `in_progress` lock + QBO delta-record; the wholesale `process-pending-payments` excludes `isDropship` from all 3 passes. `services/dropship/dropshipPayment.{config,service}.js` + `services/scheduler/jobs/processDropshipPayments.job.js`. Legacy `admin_order` rows (pre-invoicing) are untouched (no backfill). Surfaced on the **Admin Orders** pages; the detail page (`app.admin-orders.$id.jsx`) has parity with the wholesale Order Details invoice panel — live QBO invoice view (shared `app/utils/qboInvoice.utils.projectQboInvoice`), **View PDF**, **Send invoice** email, **Collect payment now** (retry-payment endpoint's drop-ship branch injects `DROPSHIP_NMI_VAULT_ID`), plus Email/Attempt/Remarks history. Spec: INTEGRATIONS.md §5.6 / §10.6. |
| Drop-ship fulfillment status mirror (Wholesale → Retail) | ✅ | When a drop-ship wholesale order is **fulfilled / shipped / tracking-updated / delivered / cancelled**, the status (carrier + tracking + delivery state) is mirrored onto the **linked retail Shopify order**. Reverse of the retail→wholesale intake sync. Wholesale: `services/sync/fulfillmentSync.notifyRetailOfDropshipChange` is called from `handleFulfillmentUpdate` + `syncFulfillmentsFromShopify` + `handleOrderCancelled` (after `pushShippingToInvoice`) — **gated** on a `DropshipMapping` existing (the cross-store link: `retailOrderId/Gid/Shop` ↔ `wholesaleOrderId`), **deduped** on a content `signature` stored in the mapping's new `retailFulfillmentSync` audit block, **best-effort** (never throws into the fulfillment path). POSTs to `${NS_RETAIL_API_BASE}/api/sync/wholesale-fulfillment` with `x-sync-secret` (`RETAIL_SYNC_SECRET`); `isFulfillmentSyncEnabled()` gates on base URL + secret. ns-retail: `POST /api/sync/wholesale-fulfillment` → `services/sync/wholesaleFulfillment.applyWholesaleFulfillment` creates the retail Shopify fulfillment (`fulfillmentCreate`, all open fulfillment orders, `notifyCustomer: true`) or updates tracking (`fulfillmentTrackingInfoUpdate`, customer notified only when the tracking NUMBER changed), then reuses `recordFulfillmentAndSync` (DB + retail QBO invoice + shipment email). Cancellation = tag `wholesale-cancelled` (no auto-refund). **Delivered milestone:** wholesale stamps `fulfillments[].deliveredAt` + order-level `deliveredAt` at the `delivered` transition and syncs the delivery date; ns-retail records `delivered` + `deliveredAt` onto `cdo_orders` **without** a Shopify re-write (carrier status isn't API-settable) and the derived Delivery badge/date reflect it. Spec: INTEGRATIONS.md §5.7. |
| Practitioner Portal (Customer Account UI extension, full-page) | ➡️ MOVED to ns-retail | **No longer part of this app** (as of 2026-06-08). The portal is a CDO-program feature and ns-retail owns + writes the `cdo_*` collections, so the whole feature — the Customer Account UI extension AND its `/api/portal/*` backend + `cdo.portal.service.js` — was relocated to the **ns-retail** workspace, where it reads the real cdo models instead of wholesale's read-only mirrors. Removed from wholesale: `extensions/practitioner-portal-account/`, `app/api/portal/*`, `services/cdo/cdo.portal.service.js`, and the 4 portal-only mirror models (`cdo{Order,Commission,Payout,Referral}.server.js`). Spec now lives in `ns-retail/docs/payout.md §18`. |

## Changelog

- **2026-06-17** — **Delivered milestone + delivery date now sync Wholesale → Retail (builds on the fulfillment mirror below).** When a drop-ship wholesale order reaches the carrier **`delivered`** status, the linked retail order is marked delivered with the **delivery date** synchronized. **Wholesale capture:** `ShopifyOrder.fulfillments[].deliveredAt` (new) is stamped the first time a shipment's `shipment_status` is `delivered` (first-detection-wins — prefers the webhook `updated_at`, else now), and a new `recomputeDeliveredAt` sets order-level `ShopifyOrder.deliveredAt` (new) once **every** active shipment is delivered (mirrors `recomputeShipDate`; runs in both `handleFulfillmentUpdate` + `syncFulfillmentsFromShopify`). The delivered transition flips the existing sync signature, so a POST fires carrying per-shipment + order-level `deliveredAt` (+ a `delivered` flag) in the `fulfillmentSync` payload. **ns-retail apply:** since carrier `shipment_status` **can't** be set via the Admin API, a delivered/status-only change (tracking number unchanged) now **skips the Shopify mutation entirely** (re-sending identical tracking would be a pointless customer email) and records `shipmentStatus: 'delivered'` + `deliveredAt` straight onto `cdo_orders.fulfillments[]` via the existing `recordFulfillmentAndSync` (logged as `wholesale_delivered`; the prior cut did a spurious tracking re-write here). New `cdo_orders.fulfillments[].deliveredAt`; `recordFulfillmentAndSync` captures `delivered_at`; `pickTracking` now also picks a status-only shipment (no number); `app/utils/orderStatus.deriveDeliveredAt` prefers the synced `deliveredAt` over `updatedAt`, so the retail **Delivery status** badge + delivery date (already derived, list + detail) reflect the wholesale delivery state + date. **Dedup/idempotency:** signature short-circuits an unchanged delivered state; `deliveredAt` is first-write-wins on both sides, so repeated syncs never move the recorded date. No new env. Spec: INTEGRATIONS.md §5.7 (Delivered milestone).

- **2026-06-17** — **Drop-ship fulfillment status now mirrors Wholesale → Retail (cross-repo, `wholesale` + `ns-retail`).** When the Wholesale Admin fulfills a drop-ship order — and on every later shipment change (tracking added, carrier change, **delivered**) plus **cancellation** — that status is pushed onto the **linked retail Shopify order** so the retail customer's order reflects the real fulfillment. Reverse direction of the existing retail→wholesale intake sync; ns-retail owns the retail Shopify token, so wholesale **notifies** and ns-retail **applies**. **Mapping:** the existing `dropship_mappings` doc is the cross-store link (`retailOrderId`/`retailOrderGid`/`retailShop` ↔ `wholesaleOrderId`); it gains a `retailFulfillmentSync` audit block (`lastSignature`, `lastEvent`, `lastStatus`, `lastSyncedAt`, `lastError`/`lastErrorAt`, `attempts`) for status-tracking + dedup. **Wholesale side:** new `services/sync/fulfillmentSync.service.notifyRetailOfDropshipChange({ localOrder, event })`, called (best-effort, never throws) from the two fulfillment choke-points (`handleFulfillmentUpdate` webhook + `syncFulfillmentsFromShopify` live-pull) and `handleOrderCancelled`, right after `pushShippingToInvoice`. **Gated** on a `DropshipMapping` existing (so only retail-triggered drop-ship orders are mirrored — ordinary wholesale orders are skipped) and **deduped** on a content signature of the fulfillment state (so re-delivered webhooks / repeated Order-Details views never re-POST a duplicate or conflicting update). POSTs to `${NS_RETAIL_API_BASE}/api/sync/wholesale-fulfillment` with the shared `x-sync-secret`; `sync.config.isFulfillmentSyncEnabled()` gates on `NS_RETAIL_API_BASE` + `RETAIL_SYNC_SECRET`. **ns-retail side:** new endpoint `app/api/sync/wholesale-fulfillment.js` (shared-secret auth, module-level dedup, fire-and-forget; registered in `app/routes.js`) → new `services/sync/wholesaleFulfillment.service.applyWholesaleFulfillment`: resolves the `cdo_orders` doc + `unauthenticated.admin(retailShop)`, then for a **fulfillment** event queries the order's `fulfillmentOrders`/`fulfillments` and either `fulfillmentCreate`s over all open fulfillment orders (carrier + tracking, `notifyCustomer: true` → Shopify shipping email) or `fulfillmentTrackingInfoUpdate`s the latest fulfillment (customer notified **only when the tracking number changed**, so carrier-driven "delivered"-style status pushes don't re-email), then records it onto `cdo_orders` + the retail QBO invoice via the existing `recordFulfillmentAndSync` (the retail store's own `fulfillments/*` webhook from our mutation is then an idempotent re-run). A **cancellation** event tags the retail order `wholesale-cancelled` (safe, non-destructive) and logs it — it deliberately does **NOT** auto-cancel/refund the paid retail order (manual money decision). All steps append to `cdo_orders.retailQbo.syncLog`. Shopify mutations validated against the Admin schema (the `*V2` forms are deprecated → `fulfillmentCreate` / `fulfillmentTrackingInfoUpdate`); needs `write_*_fulfillment_orders` + `write_orders` on the retail store. New env: wholesale `NS_RETAIL_API_BASE` (mirror of ns-retail's `WHOLESALE_API_BASE`); empty = safe no-op. Spec: INTEGRATIONS.md §5.7.

- **2026-06-17** — **Restored the missing `scripts/sync-extension-app-url.js` build script** (was referenced by the `sync:extension-app-url` / `predev` / `predeploy` npm hooks but never committed, so `npm run dev` and `npm run deploy` both crashed with `MODULE_NOT_FOUND`). The script bakes the current app URL into the Customer Account UI extension's HTTP client — `extensions/services/FullPageApi.jsx`'s hardcoded `const SERVER_URL = "…"`. That extension runs in a Web Worker sandbox with **no `process.env`**, so the absolute backend URL it fetches (`${SERVER_URL}/api/portal/profile`) must be written into the source before bundling. URL resolution is first-match-wins: **CLI arg → `process.env.SHOPIFY_APP_URL` → `SHOPIFY_APP_URL` in `.env` → `application_url` in the active `shopify.app*.toml`** (the `.env` value is what the Shopify CLI rewrites each `shopify app dev` session). Dependency-free (its own tiny `.env` + `.toml` readers), idempotent (no-op when already in sync), and never hard-fails the pipeline (warns + exits 0 if no URL resolves or the target file is absent). **Known limitation:** `predev` runs *before* `shopify app dev` boots, so the first sync of a session bakes in the *previous* session's tunnel URL — if the trycloudflare tunnel rotated, re-run `npm run sync:extension-app-url` (or pass the URL as an arg) once the new URL is printed, or pin a stable URL via ngrok / `SHOPIFY_APP_URL`. The dead `pilot-tune-intent-keith.trycloudflare.com` value previously frozen in `FullPageApi.jsx` is now synced to the live app URL automatically.

- **2026-06-17** — **Admin Order Details → invoice parity with the wholesale Order Details page.** The drop-ship Admin Order detail page (`app.admin-orders.$id.jsx`) now shows the full invoice + payment view and actions, not just read-only order info. Added: an **Invoice & payment** summary (status/method/amounts/balance/due/attempts), a **QuickBooks invoice** panel that live-pulls the QBO invoice (product lines + SKUs + full breakdown + balance + "Open in QuickBooks"), and **Email history** / **Attempt history** / **Remarks** timelines. Actions wired to the shared `/api/admin/orders/:id/*` endpoints: **View invoice PDF** (`qbo-invoice-pdf`), **Send invoice** (`send-invoice`), and **Collect payment now** (`retry-payment`). `retry-payment.js` gained a drop-ship branch — when `invoice.isDropship` it skips the wholesale per-customer vault/ACH resolution and injects `DROPSHIP_NMI_VAULT_ID` (mirrors the CRON), so admins can collect on demand. The QBO-invoice projection was extracted from `app.orders.$id.jsx` into a shared pure util `app/utils/qboInvoice.utils.projectQboInvoice` (both pages import it). Wholesale-only actions (mark cheque paid, charge-card fallback, ACH retry, pause auto-charge/reminders) are intentionally omitted — they don't apply to the auto-collected drop-ship method. Spec: INTEGRATIONS.md §5.6.
- **2026-06-17** — **Drop-ship orders now auto-invoiced (UNPAID) + collected by a dedicated CRON.** Supersedes the 2026-06-11 "Admin Orders never invoiced" behaviour for the retail drop-ship customer (`DROPSHIP_RETAIL_CUSTOMER_EMAIL`). **Order creation:** `processShopifyOrder`'s drop-ship short-circuit now calls `invoiceDropshipOrder` instead of parking the order at `admin_order` — it `ensureDropshipCustomerMap`s a single QBO customer (find-or-create by email; NO NMI-vault sourcing and NO billing-address hard-fail, unlike the wholesale `ensureCustomerForOrder`), creates an **UNPAID** QBO invoice via `createInvoiceForOrder({ isDropship: true })` (new param locks `paymentMethod: 'dropship'` + `customerPaymentPreference: 'dropship'` and stamps `Invoice.isDropship: true`; `'dropship'` carries no processing fee and sits outside the wholesale PASS 1 card/ach filter), and sets the new terminal `ShopifyOrder.processingStatus: 'dropship_invoiced'`. Same claim-first dedup, cancellation-race void guard, and runs on the replay path. **Collection:** a dedicated CRON `process-dropship-payments` (`services/dropship/dropshipPayment.{config,service}.js` + `services/scheduler/jobs/processDropshipPayments.job.js`, `concurrency:1`, registered in `jobs/index.js` + `scheduler.service.ensureRecurring` before the retry dev-override early-return) sweeps `{ isDropship: true, paymentStatus: 'pending' }` and charges the single configured `DROPSHIP_NMI_VAULT_ID` (injected onto the customer map at charge time) via the shared `payment.service.chargeInvoice` — `paymentMethod: 'dropship'` routes through the vault's default billing like a card sale, so an approval runs the existing inline `propagateSuccessfulPayment` (QBO `recordPayment` + Shopify `orderMarkAsPaid` + local mirror → order `completed`). PASS B replays downstream sync for charged-but-unsynced rows. **Cadence:** production once per month (`DROPSHIP_PAYMENT_CRON=30 0 1 * *`), testing every 2 minutes (`DROPSHIP_PAYMENT_INTERVAL=2 minutes`). **Duplicate prevention:** claim-first unique `(shop, shopifyOrderId)` Invoice index (no duplicate invoice), `in_progress` lock + `concurrency:1` (no double-charge), QBO `amountPaid − qboRecordedTotal` delta-record (no duplicate Payment). **Segregation:** the wholesale `process-pending-payments` now excludes `isDropship: { $ne: true }` on all 3 passes (charge / failed-followup / sync-retry). **Models:** Invoice gains `isDropship` (indexed) + `'dropship'` in `paymentMethod`/`customerPaymentPreference` enums + `cron_dropship_attempt` remark kind; Order gains `'dropship_invoiced'` status (added to `TERMINAL_STATUSES`). New env: `DROPSHIP_NMI_VAULT_ID`, `DROPSHIP_PAYMENT_CRON`, `DROPSHIP_PAYMENT_INTERVAL`, `DROPSHIP_DUE_DATE`. UI: new `dropship_invoiced` + `dropship` badge tones; Admin Orders pages copy corrected (they were stating "no invoice / never processed"). Legacy `admin_order` rows are not backfilled. Spec: INTEGRATIONS.md §5.1 / §5.2 / §5.6 / §10.6 / §16.
- **2026-06-11** — **Admin Orders — separate, read-only flow for retail drop-ship orders.** Orders placed by the retail drop-ship customer (`DROPSHIP_RETAIL_CUSTOMER_EMAIL`) are already paid and must never enter the wholesale QBO/NMI pipeline or be touched by the payment/commission CRON. **Orchestrator diversion:** `processShopifyOrder` now checks the order email against the retail anchor **immediately after the atomic claim** (before validation, the approval gate, `ensureCustomerForOrder`, invoice creation, and any charge) and, on a match, sets the new terminal `ShopifyOrder.processingStatus = 'admin_order'` and returns. This runs on the replay path too, so an Admin Order can't be pulled back into the wholesale flow even if the synthetic customer were ever `Approved`-tagged. **CRON is automatically safe** — the scheduler iterates the `Invoice` collection, and Admin Orders create no invoice, so there's nothing to pick up (no CRON-side filter needed). **Detection** is centralized in `services/dropship/dropship.config.js`: pre-normalized `RETAIL_CUSTOMER_EMAIL` + `isRetailCustomerEmail(email)` (both server-only, loader-imported). **UI separation:** the wholesale Orders list (`app.orders._index.jsx`) now excludes the retail customer on both the row query and the chip-count aggregation (`customerEmail: { $ne: RETAIL_CUSTOMER_EMAIL }`; `$ne` still matches null emails so ordinary orders are unaffected); a new **Admin Orders** nav item exposes two read-only routes — `app.admin-orders._index.jsx` (list anchored on `{ customerEmail: RETAIL_CUSTOMER_EMAIL }` so it captures orders ingested before this diversion existed too, with fulfillment-status chips + search + pagination) and `app.admin-orders.$id.jsx` (full detail: order info, customer info, order tags, line items with quantities/pricing, shipping + billing addresses + shipping method, fulfillment + tracking numbers/URLs, order note + note attributes, and additional Shopify metadata). The detail page reuses the existing best-effort `syncFulfillmentsFromShopify` live-pull (its QBO-memo push is gated on `invoiceRef`, which Admin Orders never have, so it only reads Shopify + writes tracking locally) and has **no payment actions**. New `admin_order` tone added to the shared `ProcessingBadge`. No change to any existing order/invoice/commission/CRON behaviour for non-retail orders. `DROPSHIP_RETAIL_CUSTOMER_EMAIL` was already defined + read by the drop-ship config — reused as-is. Spec: INTEGRATIONS.md §5.1 / §5.2 / §5.6.

- **2026-06-10** — **`/pay` result screens: circular status icon, success/failure copy + dedicated failure screen (UI-only, `components/pay-ui.jsx` + `api/pay/pay.jsx`).** Replaced the thin accent bar on the `PayResult` shell (used by every `/pay` terminal state) with a **72px accent-filled circle + white glyph + soft tinted ring** and a reduced-motion-aware pop-in. New `StatusGlyph` picks the glyph by tone — **success → green tick**, **critical → ✕**, info/neutral → an info "i" — with `TONE_TINTS` for the ring. `PayResult.message` now renders **multiple paragraphs** (string split on blank lines, or an array) and `action` accepts an **`onClick`** (renders a `<button>`) in addition to `href`. **Success copy** set to "Payment Successful" + "Thank you! Your payment has been received and processed successfully.\n\nNo further action is required. You may now safely close this window." (already-paid state → "You're all set — this invoice is paid"). **New dedicated failure screen:** a server charge that returns `ok:false` now renders a full critical-tone screen (red ✕) titled "Payment Failed" with the server message + "No charge was made…try again / contact us" + a **"Try again"** button that reloads a fresh form (reliable Collect.js re-init). Client-side validation/tokenization/load errors stay **inline** in the form (so the customer corrects them in place) — `shownError` is now just the client `error`, the action-error path moved to the failure screen. Pure presentational — no change to charge/settlement/loader logic. esbuild-transforms clean. Spec: INTEGRATIONS.md §9.6.

- **2026-06-10** — **`/pay/:token` field-border fix — double box + on-focus dark rectangle (UI-only, `api/pay/pay.jsx`).** The Collect.js card fields showed a nested box (outer shell border + an inner border on the `<input>` *inside* NMI's cross-origin iframe) and, on click, a dark rectangle. Root cause: the inner input is only styleable via Collect.js's `customCss`/`focusCss`, which previously set just font/color/line-height — leaving the input's native (user-agent) border/background/appearance and its focus outline intact. The `.pay-field iframe { border: 0 }` rule only ever touched the iframe **element**, never the input within. Fix: matched the **proven** Collect.js style config already used by `registration-form/PaymentCardForm.jsx` (which has never shown this bug) — `customCss` now resets the inner input fully incl. **`appearance:none`** (+ `-webkit-`/`-moz-`), `border:none`, `border-radius:0`, `outline:none`, `box-shadow:none`, transparent background, zero margin/padding; `focusCss` does a thorough outline kill (`outline:0px` + `outline-width:0` + `outline-style:none` + `border:none` + `box-shadow:none`); `invalidCss`/`validCss` add `border:none` so validation states don't draw a box either. The field shell (`.pay-field`) is now the sole border owner and its `:focus-within` blue ring is the only focus indicator. `.pay-field iframe` also got `outline:0`/`box-shadow:none` belt-and-suspenders. No change to tokenization, charge, or settlement logic. **Verification:** esbuild-transforms clean; config is byte-for-byte the same approach as the working registration form. Live cross-origin iframe render must be confirmed with a hard refresh on the page (Collect.js applies `customCss` to the input at runtime). Spec: INTEGRATIONS.md §9.6.

- **2026-06-09** — **Pay link: guarantee a complete URL is generated + polished the /pay page input fields.** (1) **Link generation hardening.** Two code defects could emit an incomplete/invalid pay URL regardless of the host. `payLink.utils.buildPayLinkUrl` silently returned a **host-less relative** `/pay/<token>` when the base URL was empty/misconfigured; it now **throws** unless the base is a complete absolute `http(s)://host`, so a broken base fails loudly at issue time instead of baking a dead link into a QBO memo for days. The Order Details loader guards the call with try/catch (surfaces the misconfig in an `s-banner` instead of 500-ing); `refresh-pay-link` already returns the error as a 502. `qbo.service.setInvoicePayLinkMemo` previously built `base + payLinkBlock` then `slice(0, 1000)` to QBO's memo cap — truncating **from the end**, i.e. chopping the pay URL itself when the base memo was long (the reported "URL cut off / hyphens missing" symptom). New shared `payLink.utils.appendPayLinkToMemo` (+ `QBO_MEMO_MAX_LEN`) trims the **base memo** to fit the cap and always appends the full pay-link block (label + complete URL) intact; both the creation path (`createInvoiceForOrder`) and the refresh path now use it so they can't drift. The token stays hyphen-free base62, so the token itself can never be split by a linkifier; the remaining wrap risk is purely a long **host** (dev trycloudflare tunnels with hyphens) — operationally solved by `PAY_LINK_BASE_URL` = a short stable production host. (2) **`/pay/:token` UI polish** (`api/pay/pay.jsx`, UI-only). Added a `<style>` block (inline styles can't express pseudo-classes) giving the card fields a hover + **focus-within ring**, forcing the NMI Collect.js iframes to fill their bordered shells (removes the nested double-box look), and a button hover/active state. Refined field/label spacing, added a "Loading secure payment fields…" hint, replaced the bare red error text with a bordered error callout (dot + message), tuned the Collect.js `customCss`/`focusCss`/`placeholderCss`/`validCss` so the typed card text is vertically centred and consistent, and added a 🔒 to the footer. No change to tokenization, charge, or settlement logic. Spec: INTEGRATIONS.md §9.6.

- **2026-06-09** — **Immediate Payment: removed the QR code (link-only) + fixed the dead pay link (durable base URL + "Refresh link").** (1) **Root cause of "link not working"** — the `/pay/<token>` URL is baked into the QBO invoice memo at creation using the app's public URL at that moment. In dev, `shopify app dev` rotates the trycloudflare tunnel on every restart, so a previously-issued link points at a now-dead host (the reported invoice's link used an old tunnel that no longer resolves). **Fix:** new `PAY_LINK_BASE_URL` config (in `shopify.config.js`, falls back to `SHOPIFY_APP_URL`) so the link base can be pinned to a STABLE URL — in production `SHOPIFY_APP_URL` is already stable so no action needed; in dev, set `PAY_LINK_BASE_URL` to a stable tunnel/domain. Plus a new **"Refresh link on invoice"** admin action on Order Details → `POST /api/admin/orders/:id/refresh-pay-link` → `payLink.service.refreshImmediatePayLink` re-stamps the QBO memo's `Pay online:` line with the current URL (the opaque token / `/pay/<token>` path never changes), healing already-issued invoices after a base-URL change. The Order Details "Payment link" section already rebuilds a LIVE link from the token + current base, so admins always see/copy a working link. (2) **QR removed** per request — link only. Dropped: the QR PNG generation + QBO Attachable upload (`qbo.service.attachQrToInvoice`, `qbo.apis.qboUpload`/`rawUploadRequest`), the `qrcode` dependency, `payLink.utils.renderPayQr*`, the QR attach in `createInvoiceForOrder` + `provisionImmediatePayLink`, the Order Details inline-QR `<img>` + "QR on QBO invoice" row, and the `Invoice.qrAttachableId`/`qrAttachedAt` fields. The clickable `Pay online: <url>` line in the QBO `CustomerMemo` (auto-linkifies in the email/PDF) is now the sole CTA. No change to the Collect.js pay page, settlement, or `payTransactionIds` dedup. Spec: INTEGRATIONS.md §9.6 + env table.

- **2026-06-09** — **"Card on File" payment-recovery surfaced on Order Details (incl. Immediate Payment).** Cards are captured + vaulted at registration, so this is purely about *collecting* against the stored card when a customer doesn't pay by their preferred method — the same recovery the cheque/ACH flows already had, now also for **Immediate Payment** invoices and with explicit card-on-file visibility. Changes are **UI-only** (`app/routes/app.orders.$id.jsx`); the backend already supported it (`api/admin/charge-card.js` flips ANY non-card method → card and runs `chargeInvoice` → `propagateSuccessfulPayment`, which updates the invoice, records the PaymentAttempt, and syncs QBO + Shopify). (1) **Card-on-file status** — the loader now selects `wholesale_applications.payment.card` and returns `cardOnFile = { hasVault (from resolveCustomerVaultId), brand, last4 }`; the Invoice & payment section shows "Card on file: <brand> •••• <last4>" (or "None"). We never store the PAN — brand/last4 come from the registration metadata. (2) **Charge action broadened** — `showChargeCard = isManualInvoice || isImmediateInvoice` (was cheque/ACH only); the "Charge card on file" secondary action + its confirmation modal now appear for Immediate Payment invoices too (card invoices already use "Retry payment", which charges the same vault). Modal copy generalized (no longer cheque-specific; shows the •••• last4). (3) **"No card on file" warning** broadened from cheque/ACH to all recovery-eligible methods. No new endpoints, models, or NMI calls. **Save/update card from the admin was explicitly descoped** — cards are collected at registration. Spec: INTEGRATIONS.md §9.4 (charge-card recovery) + §9.6.

- **2026-06-09** — **Immediate Payment hosted-page fix: NMI 3-Step Redirect → Collect.js.** The first cut of the self-pay page minted an NMI 3-Step Redirect session and 302-redirected the browser to the returned `form-url`. That failed in testing with **"The ccnumber field is required"** on completion: NMI's 3-step `form-url` is a **POST target for your own card `<form>`**, not a hosted UI you redirect to — so a plain browser GET captured no card. Reworked the page to **NMI Collect.js** (the same iframe-tokenization mechanism the registration form already uses): `/pay/:token` (`api/pay/pay.jsx`) now renders Collect.js card fields, tokenizes the card client-side (card data never touches our server), and its `action` charges `nmi.service.chargeWithPaymentToken` (`type=sale` + `payment_token`) for the server-recomputed outstanding balance, then settles via the unchanged `settleHostedPayment` → `propagateSuccessfulPayment`. **Removed** the 3-step plumbing: `nmi.service.createHostedPaymentSession`/`completeHostedPayment`, `nmi.apis.nmiThreeStep`, `nmi.utils.xmlEscape`/`extractXmlTag`, `nmiConfig.hostedCheckoutUrl` + `NMI_BASE_URLS[*].threeStep` + the `NMI_HOSTED_CHECKOUT_URL` env, and the `/pay/return` + `/pay/cancel` routes (`api/pay/{redirect,return,cancel}.jsx` deleted). **Added** `nmi.service.chargeWithPaymentToken`, `nmiConfig.collectJsUrl` (env `NMI_COLLECT_JS_URL`, per-env default) — the page loads it with the existing publishable `NMI_PUBLIC_KEY` tokenization key. `Invoice.payToken`/QR/memo wiring + `settleHostedPayment` (QBO Payment + Shopify mark-paid + order/invoice update + dedup on `payTransactionIds[]`) are unchanged. **"Card on File"** (save/update a vaulted card from Order Details) was requested in the same round but is **intentionally deferred** to a later change. Spec: INTEGRATIONS.md §9.6.

- **2026-06-09** — **Immediate Payment — self-pay link + QR on QuickBooks invoices.** New 4th payment preference `immediate`: instead of an auto-charged card-on-file or a cheque/ACH wait, the customer receives a QBO invoice carrying a clickable **payment link** + a **QR code**, both leading to an NMI-hosted payment page pre-set to the **exact outstanding balance**; on success the invoice settles like any other payment (QBO `Payment` + Shopify mark-paid). **Architecture:** the invoice carries an opaque, unguessable `Invoice.payToken` (no amount in it); the durable public route **`GET /pay/:token`** (no Shopify auth — security is the token + server-side amount derivation) recomputes the outstanding balance and mints a **fresh** NMI 3-Step Redirect hosted session at click time (`nmi.service.createHostedPaymentSession` → `form-url`, 302 redirect), decoupling the long-lived QBO link from NMI's short session lifetime. **`GET /pay/return`** completes the 3-step (`completeHostedPayment` — the authoritative settlement signal; the browser redirect alone is never trusted) and settles via new `services/payment/payLink.service.settleHostedPayment`, which reuses the existing idempotent `propagateSuccessfulPayment`. Settlement is deduped on the NMI transaction id (`Invoice.payTransactionIds[]`, atomic `$addToSet` claim) so a refresh/double-callback can't double-record. **QBO side:** the pay URL goes into the `CustomerMemo` as a bare `Pay online: <url>` line (auto-linkifies in email/PDF — QBO can't embed an inline image), and the QR PNG is attached to the invoice via the new Attachable `/upload` multipart transport (`qbo.apis.qboUpload` + `qbo.service.attachQrToInvoice`, `IncludeOnSend:true`) so it travels with the emailed invoice — both wired into `createInvoiceForOrder` BEFORE the Phase-4 `/send`, best-effort so a QR/upload failure never breaks invoicing. **Fee:** `INVOICE_FEE_RATE_IMMEDIATE` (default 3%, baked into `amountDue` at creation like card; label "Payment Processing Fee"). **Due date:** `IMMEDIATE_DUE_DATE`. **Admin:** Order Details shows the link + inline QR (`renderPayQrDataUrl`) + copy button + payment status; realigning an open invoice to `immediate` via `paymentPreference.service` retro-provisions its link/QR (`provisionImmediatePayLink` + `qbo.service.setInvoicePayLinkMemo`). New: `Invoice` fields `payToken`/`payTokenCreatedAt`/`qrAttachableId`/`qrAttachedAt`/`payTransactionIds`; enum value `immediate` on `Invoice.paymentMethod`/`customerPaymentPreference`/`processingFeeMethod` + `CustomerMap.paymentMethod`; `PaymentAttempt.outcome` value `hosted_paid`; config `NMI_HOSTED_CHECKOUT_URL` (3-step endpoint, derived per-environment, overridable); `qrcode` dependency; modules `services/payment/payLink.{utils,service}.js`, routes `app/api/pay/{redirect,return,cancel}.jsx`, component `app/components/pay-ui.jsx`. **Scheduler unchanged** — PASS 1 already filters `card`/`ach`, reminders are cheque-only, so `immediate` is auto-excluded from every CRON. **Scope notes:** (1) the **registration-form UI** for selecting Immediate Payment is intentionally **NOT** part of this change (handled separately) — set the preference today via admin `POST /api/admin/customers/:id/payment-method`; `normalizePaymentMethod` + the model enums already accept `immediate`. (2) The exact **NMI hosted product** (3-Step Redirect assumed) + endpoint/key must be confirmed for the account before go-live (`NMI_HOSTED_CHECKOUT_URL`); the integration is pluggable behind `nmi.service.createHostedPaymentSession`/`completeHostedPayment`. (3) The QR arrives as an email **attachment** (QBO has no inline-image API), with the clickable memo link as the primary CTA. Spec: INTEGRATIONS.md §9.6.

- **2026-06-08** — **Practitioner Portal MOVED OUT to the ns-retail app.** The portal is a CDO-program feature and `ns-retail` owns + writes the `cdo_*` collections it reads, so the entire feature was relocated there (single-owner architecture). **Removed from this workspace:** the Customer Account UI extension `extensions/practitioner-portal-account/` (git-moved to `ns-retail/extensions/`), the `/api/portal/*` handlers + guard (`app/api/portal/*`), `services/cdo/cdo.portal.service.js`, the 8 portal route registrations in `app/routes.js`, and the 4 portal-only read-only mirror models (`app/models/cdo{Order,Commission,Payout,Referral}.server.js`). **Kept:** `models/cdoPractitionerCode.server.js` (still used by this app's own `cdo.service.js`), `models/wholesaleApplication.server.js` (app-wide), and `services/APIService/*` (other endpoints use it). In ns-retail the portal now reads the **real** cdo models (not mirrors) and uses `db/mongo.server` for `connectDB`; the extension's `api_base_url` must point at the **ns-retail** app URL, and the ns-retail Partner app needs customer accounts + protected customer data access. Canonical spec moved to `ns-retail/docs/payout.md §18`; `INTEGRATIONS.md §25` here is now a historical pointer. The earlier 2026-06-08 portal entries below describe the feature as it existed in this app before the move.

- **2026-06-08** — **Practitioner Portal: per-payout commission breakdown + UI polish.** (1) **Payout breakdown** — each payout in the Payouts tab is now an expandable card (Date / Amount / Method / Status / Reference / Transaction) with an `s-details` disclosure revealing the order-level commissions that make up that payout: **Order ID, Order date, Patient, Revenue, Commission %, Commission amount, Status**. Backend joins `cdo_commissions` (linked via `payoutId`) → `cdo_orders` (order date / `customerName` / revenue) in a new `getPayoutBreakdowns(practitionerId, payoutIds)` helper, attached as `row.breakdown[]` by `getPayouts` (one extra aggregation per page, scoped by `practitionerId` so a practitioner only sees their own payouts' lines — no new endpoint). `PayoutsSection` rewritten from a flat table to breakdown cards. (2) **Badge tones fixed** — customer-account `s-badge` only supports `tone="auto|neutral|critical"`; the shared `StatusBadge` previously passed `success`/`warning`/`info` (invalid → everything rendered grey). Now failures (`failed`/`rejected`/`declined`/`error`/`cancelled`) → `critical`, else `neutral`, with Title-Cased labels. NOTE: this surface has **no green/amber badge tone** — status differentiation is by label text + red for failures. (3) **Underlined tab bar** — replaced the `s-button` nav row with a `Tabs` component (no native Tabs on this surface; built from `Clickable` + `Box` bottom-border indicator). (4) Removed the redundant "Commission status" column from the Commissions table (Order / Amount / Rate / Payout status / Earned on). Files: `services/cdo/cdo.portal.service.js`, `extensions/practitioner-portal-account/src/{sections,ui,PractitionerPortal}.jsx`.

- **2026-06-08** — **Fixed the Practitioner Portal Customer-Account extension rendering a blank page (APIs all 200, nothing painted).** Root cause: `extensions/practitioner-portal-account/` had **no `tsconfig.json`**, so the Shopify CLI's esbuild defaulted the JSX *automatic runtime* to **React**. `react` is hoisted in `node_modules` (from the main app), so `react/jsx-runtime` resolved and the build **succeeded silently** — but at runtime the JSX produced **React elements** while `render` is imported from **Preact** (`src/PractitionerPortal.jsx`). Preact's `render()` can't mount React elements → the bundle loaded, `shopify.sessionToken.get()` worked, every `/api/portal/*` call returned 200, yet the page stayed blank (failure is purely in the render layer, not auth/data). Fix: added `extensions/practitioner-portal-account/tsconfig.json` with `"jsx":"react-jsx"` + `"jsxImportSource":"preact"` (matching the working `ns-retail/extensions/checkout-ui-code/tsconfig.json`), so esbuild routes JSX to `preact/jsx-runtime`. Verified via the build metafile — `dist/*.metafile.json` inputs now list `preact/jsx-runtime` and **no** `react` (bundle ~79KB → ~37KB). **Requires a `shopify app dev` restart** (esbuild doesn't pick up a newly-created tsconfig mid-session). Also (dev-workflow): added `extensions/practitioner-portal-account/src/config.js` `DEV_API_BASE_URL` manual override (wins over the merchant-set `api_base_url` when non-empty) so the rotating trycloudflare tunnel URL can be pasted once per dev session instead of editing the Customer-Account editor; leave it `''` for production builds.

- **2026-06-08** — **Practitioner Portal moved into the Customer Account (full-page UI extension); storefront theme block removed.** The portal now renders as a dedicated page inside the customer account via a new Customer Account UI extension (`extensions/practitioner-portal-account/`, `type = "ui_extension"`, single `customer-account.page.render` target, `network_access = true`, api_version `2025-10`). Built with **Preact + Polaris web components** (`s-page`/`s-section`/`s-grid`/`s-stack`/`s-text`/`s-badge`/`s-button`/`s-text-field`/`s-select`/`s-divider`/`s-spinner`) — no custom HTML/CSS (sandbox forbids it); tables are built from `s-grid`. **Auth front-door swapped** from the App-Proxy `logged_in_customer_id` to a **session-token JWT**: the extension calls `shopify.sessionToken.get()` and sends `Authorization: Bearer`; `app/api/portal/_guard.js` now verifies via `authenticate.public.customerAccount` (built into `@shopify/shopify-app-react-router`) and resolves the token's `sub` (customer GID) → approved `WholesaleApplication._id` = `practitionerId` via `resolvePractitionerByCustomerGid` in `cdo.portal.service.js`. CORS preflight (`OPTIONS`) is answered by `portalAction` on every portal route (the `Authorization` header makes the fetch non-simple). **Everything else reused unchanged** — the 8 `/api/portal/*` endpoints, all aggregations, and the 5 `cdo*` mirror models. The extension's backend base URL comes from a merchant-set `api_base_url` extension setting. **Removed** (consolidation): the storefront `practitioner-portal/` React SPA workspace, `extensions/theme-extension/blocks/practitioner_portal.liquid`, the `practitioner-portal-bundle.*` assets, and the `build:portal` script (+ its `predeploy` hook). **Known limitation:** CSV export dropped on this surface (no DOM/Blob in the Web Worker sandbox) — a later phase can serve CSV behind a short-lived signed URL. **Prerequisites:** new customer accounts + protected customer data access (for the `sub` claim) — confirmed in place. **Merchant step:** add the page to the customer-account navigation menu and set `api_base_url` in the customer-account editor. Spec: INTEGRATIONS.md §25.

- **2026-06-08** — **Practitioner Portal (storefront theme app extension) — Phase 1 foundation.** New self-service dashboard letting CDO practitioners track referred patients, revenue, commissions, payouts, referral codes, and discounts. **Read-only** over the shared `cdo_*` MongoDB collections written by `ns-retail` — no commission/payout *generation* here (that stays in ns-retail). New read-only mirror models `app/models/cdo{Order,Commission,Payout,Referral,Application}.server.js` (`strict:false`, header-flagged as ns-retail-owned, like `cdoPractitionerCode.server.js`). All aggregation/query logic in new `services/cdo/cdo.portal.service.js`; thin App-Proxy handlers in `app/api/portal/*` (`me`, `summary`, `revenue`, `customers`, `commissions`, `payouts`, `referrals`, `discounts`) registered in `routes.js`, sharing a `_guard.portalLoader` wrapper. **Security model:** identity is never trusted from the client — `resolvePractitioner` validates the App-Proxy HMAC, reads the signed `logged_in_customer_id`, maps it to an **approved** `WholesaleApplication` (whose `_id` IS the `practitionerId` used across every `cdo_*` collection), and scopes every query by it; 401 → SPA redirects to login, 403 → access-restricted notice. Frontend is a **third Vite workspace** `practitioner-portal/` (plain React 19 + scoped CSS, mirrors the `registration-form/` build pattern) emitting a **distinct** `practitioner-portal-bundle.*` into the theme extension `assets/` (`emptyOutDir:false` so it coexists with `react-app-bundle.*`); asset-base global `window.__PORTAL_EXT_ASSET_BASE__`. New theme block `practitioner_portal.liquid` (settings: `proxy_base`, `login_page_url`, `support_email`). New scripts `build:portal` + folded into `predeploy`. Summary cards + date-range filters + pagination + search + client-side CSV export; mobile-responsive (tables collapse to stacked cards < 720px). Deferred to later phases: live Shopify Discount API objects (Phase 1 derives discounts from practitioner codes), charts/graphs. Spec: INTEGRATIONS.md §25. **Note:** this file (`CLAUDE.md`) has pre-existing unresolved git merge-conflict markers (around the "Module boundaries" / "Gotchas" sections) left from an earlier merge — untouched by this change; flag for cleanup.

- **2026-06-05** — **SKU shown only in the dedicated column + fixed wrong-SKU bug.** (1) Removed the SKU from the invoice line **description** (`formatLineDescription` no longer appends `SKU: …`) so the Products column shows just the product name and the SKU appears only in QBO's dedicated SKU column (was duplicated in both). (2) **Bug fix:** products that share a display name but differ by SKU were all showing the FIRST line's SKU — because QBO Item `Name` must be unique, the create collided and the duplicate-Name fallback adopted the wrong item, which also poisoned the `qbo_item_maps` cache. Fixes: `sanitizeItemName` now appends the SKU so each SKU gets a **distinct** uniquely-named Item; the duplicate-Name adopt path only reuses an item whose `Sku` **matches**; and the cache now records `qboSku` and a hit is trusted only when `qboSku === sku` — rows missing/mismatching it (the poisoned ones) are re-resolved and overwritten, so the cache **self-heals** with no manual cleanup. The in-app Order Details "Line items" table already had a clean separate SKU column (unchanged); no order-export feature exists. Existing invoices aren't backfilled — re-test with a fresh order. Spec: INTEGRATIONS.md §7.

- **2026-06-05** — **SKU column on QBO invoices — per-product QBO Items.** QBO sources the invoice SKU column from the line's referenced `Item.Sku` (no per-line SKU field), and every line previously used one generic default item, so the column was blank. New invoices now resolve each **product** line's SKU to a per-product QBO **Item** that carries it. New `qbo.service.findOrCreateItemBySku({ sku, name })` (mirrors `findOrCreateCustomer`): cache (`qbo_item_maps` model, `sku` unique) → `findItemBySku` (QBO QL `WHERE Sku=…`) → `createItem` (`POST /item`, `Type:'Service'`, `Sku`, sanitized unique `Name`, `IncomeAccountRef`). Income account is derived once from the default item's `IncomeAccountRef` (`resolveIncomeAccountRef`, cached), with optional `QBO_INCOME_ACCOUNT_ID` fallback. `createInvoice` resolves product lines (those carrying a `sku` — `shopifyLinesToQboLines` now passes `sku`+`name` through) and sets `line.qboItemId`; `toInvoiceLine` already honored it. **Best-effort + graceful**: any lookup/create failure (or missing SKU) leaves the line on the default item, so invoicing never breaks. On `createItem` duplicate-Name it adopts the existing item. Shipping / discount / processing-fee lines stay on the default item. **Forward-only** (existing invoices not backfilled); requires the SKU to exist on the Shopify variant. Spec: INTEGRATIONS.md §7 (per-product items / SKU column).

- **2026-06-05** — **Tracking on the QBO invoice header (below Ship Date) + tracking number clickable.** Tracking now also writes to QBO's **native `TrackingNum`** invoice field (carrier + number per shipment, joined for multi-shipment), which renders in the invoice header next to **Ship Date / Ship Via** — i.e. "tracking details below the Ship Date" — distinct from the CustomerMemo message block at the top. `setInvoiceShipping` gained a `trackingNum` param + a **no-op guard** (skips the POST when memo/ShipDate/TrackingNum all already match), and `syncFulfillmentsFromShopify` now calls `pushShippingToInvoice` on every live-pull (not only on change) so the native `TrackingNum`/`ShipDate` **backfill** onto invoices synced before those fields existed, then converge (guard prevents repeat writes). On the Order Details "Shipment tracking" section the **tracking number** itself is now an `<s-link target="_blank">` to the carrier page (was plain text; carrier was the link); the in-app QBO invoice Shipping block already linked it. The CustomerMemo still carries `Track: <url>` (bare URL — auto-linkified by most PDF/email clients; QBO memo is plain text so no true styled hyperlink there). Per-shipment Ship date / carrier / tracking render for every fulfillment. Spec: INTEGRATIONS.md §4.6.

- **2026-06-05** — **Ship Date sourced from Shopify (fulfillment date), shown consistently.** The official **Ship Date** is now the Shopify fulfillment date, not the order-creation date. New denormalized `ShopifyOrder.shippedAt` = the **earliest** fulfillment date across `fulfillments[]` (recomputed via `order.service.recomputeShipDate` in both fulfillment paths). It's pushed to the **QBO invoice's native `ShipDate`** field on every fulfillment sync — the QBO helper was extended (`setInvoiceShippingMemo` → **`setInvoiceShipping({ qboInvoiceId, lines, shipDate })`**, one sparse update sets both the CustomerMemo shipping block and `ShipDate`), overwriting the order-date `ShipDate` set at invoice creation. Displayed consistently as **"Ship date"** on Order Details (Overview KV = `shippedAt`), the in-app **QuickBooks invoice** Shipping block (`shippedAt`), and the **Shipment tracking** section (per-shipment `fulfilledAt`, relabeled from "Fulfillment date" — so partially-fulfilled orders show each shipment's own date). Spec: INTEGRATIONS.md §4.6.

- **2026-06-05** — **Shipment tracking reliability fix — live-pull fallback + invoice display.** Tracking wasn't appearing for fulfilled orders because the `fulfillments/*` webhooks are unreliable here (protected-topic approval gate; and no backfill for orders fulfilled before the subscription existed). Added a **live pull** of the order's fulfillments via Shopify Admin GraphQL: new `QUERY_ORDER_FULFILLMENTS` + `shopify.service.getOrderFulfillments({ admin, shop, shopifyOrderId })` (returns normalized fulfillments + order `displayFulfillmentStatus`), and `order.service.syncFulfillmentsFromShopify({ shop, shopifyOrderId, admin })` which reuses a new shared `applyFulfillmentToOrder` upsert (the webhook path now uses it too, so push + pull stay in lockstep). The **Order Details loader** (`app.orders.$id.jsx`) now captures the `admin` client and calls this **best-effort** (a Shopify outage never 500s the page — mirrors the live-QBO-invoice fetch), so tracking renders on view regardless of webhook delivery. New `ShopifyOrder.fulfillments[]` fields `fulfilledAt` (Shopify fulfillment date) + `estimatedDeliveryAt`. Order Details now shows **Fulfillment status** + per-shipment **Fulfillment date** (+ est. delivery); the in-app **QuickBooks invoice** panel now shows a **Shipping** block (carrier + tracking number as a deep-link, one row per shipment). **The carrier + tracking is also written to the customer-facing QBO invoice** — `qbo.service.setInvoiceShipping({ qboInvoiceId, lines, shipDate })` sets a managed "Shipping:" block in the invoice `CustomerMemo` (sparse update, SyncToken guard, replaces-not-duplicates, base memo preserved, 1000-char clamp); `order.service.pushShippingToInvoice` builds the lines from the order's fulfillments and is called best-effort from BOTH fulfillment paths whenever tracking changes (never breaks tracking capture). The updated memo shows on the next invoice view/PDF; auto-resend of the invoice email is intentionally NOT done (avoids spam on multi-status updates — admins use the existing "Send invoice" button to push it). Multi-shipment supported (one entry per fulfillment id). Spec: INTEGRATIONS.md §4.6.

- **2026-06-05** — **Shipment tracking capture + display.** New `fulfillments/create` + `fulfillments/update` webhooks feed `services/order/order.service.handleFulfillmentUpdate` (mirrors `handleOrderCancelled`: HMAC verify → fire-and-forget → 200; idempotent via `seenWebhookIds[]`). It captures carrier + tracking number + `shipment_status` + a resolved **carrier deep-link** onto the local order: new `ShopifyOrder.fulfillments[]` (current state, one entry per Shopify fulfillment id, upserted in place), `trackingHistory[]` (append-only, one row per detected change), and `trackingUpdatedAt`. Carrier-link resolution lives in a pure/isomorphic `app/utils/shipping.constants.js` (`normalizeCarrier`, `CARRIER_TRACKING_URL_TEMPLATES` for UPS/FedEx/USPS/DHL, `resolveCarrierTrackingUrl`, labels) — the **service** resolves + stores the final URL so the render imports no env/config; env-configured extra carriers come from a server-only `services/order/tracking.config.js` (`CARRIER_TRACKING_URLS` JSON). Falls back to Shopify's own `tracking_url` for unknown carriers. The embedded-admin **Order Details page** (`app.orders.$id.jsx`) gains a "Shipment tracking" section: carrier name + "Track shipment" as `<s-link target="_blank">` deep-links (number pre-filled), a `ShipmentStatusBadge` (new in `components/admin-ui.jsx`), per-fulfillment last-updated, and a newest-first tracking-history table. Best-effort `system_note` remark on the linked invoice per change. Webhooks registered both declaratively (both `shopify.app*.toml`) and programmatically (`REQUIRED_SUBSCRIPTIONS` — `FULFILLMENTS_CREATE`/`FULFILLMENTS_UPDATE`, approval-gated like `orders/create`); `read_orders` scope already covers fulfillment data. No customer-facing storefront portal (out of scope — the admin Order Details page is the order view today). Spec: INTEGRATIONS.md §4.6 (fulfillment tracking).

- **2026-06-05** — **Payment-preference change now realigns all of a customer's open invoices.** When a customer's method changes (card / ACH / check), every **unpaid/open** invoice (`paymentStatus ∈ {pending, failed}`, `amountPaid == 0`, `qboCreationStatus:'created'`) is realigned to the new method: the per-method **processing fee** is recomputed on the pre-fee base (`amountDue − processingFeeAmount`) and the QBO fee line is **rewritten** (added / replaced / removed), the per-method **due date** (`DueDate` + local `dueAt`) is recomputed via `dueDaysForMethod`, and `failed` invoices reset to `pending` (attemptCount 0) so the new method's auto-charge flow resumes. `partially_paid` / `paid` / `in_progress` / `awaiting_settlement` / `cancelled` are never touched. New service `services/invoice/paymentPreference.service.applyPaymentPreferenceToOpenInvoices({ shop, email, newMethod, performedBy, source })` (per-invoice try/catch isolation, mirrors `replayPendingOrdersForCustomer`); it also mirrors the new method onto `CustomerMap.paymentMethod` and appends a change-event entry to the new `WholesaleApplication.paymentMethodHistory[]` audit array (previousMethod, newMethod, invoiceCount, affectedInvoiceIds, changedAt, performedBy, source) **plus** an `admin_action` remark on each realigned invoice. New QBO helper `qbo.service.setInvoiceProcessingFee({ qboInvoiceId, feeLine, dueDate })` does a sparse `Line`-array rewrite (strips existing `/Processing Fee/i` lines, adds the new one or none) + `DueDate` in one call — the `SyncToken` from the GET is the concurrency guard against a racing CRON charge. Triggers: **customer self-service** via `/api/update-profile` (best-effort, never fails the profile save; performedBy = customer) and **admin** via new `POST /api/admin/customers/:id/payment-method` (performedBy = admin email) surfaced as a method selector + "Apply to open invoices" button + history table on the Customer detail page (`app.customers.$id.jsx`). `normalizePaymentMethod` moved from `customer.service.js` to `customer.utils.js` for reuse. **Future orders already used the latest preference** (verified: `ensureCustomerForOrder` re-syncs `CustomerMap.paymentMethod` at intake) — no scheduler change needed; PASS 1's `paymentMethod:{ $in:['card','ach'] }` filter adjusts automatically. Known limitation: switching an invoice to ACH does not provision an NMI ACH billing profile — if absent, the scheduler's ACH charge skips with a reason (existing `resolveInvoiceVault` behavior); `customerPaymentPreference` (immutable order-time snapshot) is intentionally not rewritten. Spec: INTEGRATIONS.md §7.3.1 (payment-preference realignment).

- **2026-06-05** — Moved **Tax off the QBO invoice's Products section and into its native summary "Tax" row.** `shopifyLinesToQboLines` no longer emits a `Tax` `SalesItemLineDetail`; instead `createInvoiceForOrder` passes `order.total_tax` (tax is **sourced from Shopify**, not QBO) to `qbo.service.createInvoice`, which **always** sets `TxnTaxDetail.TotalTax` (even at $0) so the customer sees a tax figure on every invoice, alongside Subtotal / Discount / Total. By design no QBO tax code (`TxnTaxCodeRef`) is applied — tax is configured in Shopify. Caveat: QBO's template may still omit a **$0.00** tax row (its rendering is partly gated on a tax code); a non-zero Shopify tax renders fine, and the app's own Order Details panels always show the tax line regardless. The invoice total is unchanged (QBO adds `TotalTax` to the line subtotal, reconciling with Shopify's `total_price`). **Scope note / QBO constraint:** the original request was to move *Shipping, Tax, and the Processing Fee* out of the Products section into the summary. The customer invoice is rendered entirely by QuickBooks (emailed via `/invoice/{id}/send`, PDF via `/invoice/{id}/pdf`) — the app only feeds a `Line[]` array. QBO's summary has native slots **only** for Subtotal, Discount (`DiscountLineDetail`, already used), Tax (`TxnTaxDetail`), and Total. Confirmed against the QBO Invoice entity that there is **no API field for a shipping amount** (`ShipMethodRef`/`ShipDate`/`ShipAddr` are metadata only) and **no slot for a custom processing-fee surcharge**, so **shipping and the processing fee necessarily remain line items**; only tax could move without building an app-rendered invoice template (deferred). Most wholesale orders are tax-exempt so this is largely cosmetic; **US automated-sales-tax companies may recompute and ignore the `TotalTax` override** (resolves to $0 for tax-exempt customers anyway). The `appendInvoiceLines` settlement-fallback uses a sparse update without `TxnTaxDetail`, so the tax override is preserved when the fee line is appended. No NMI/idempotency/scheduler changes; no data migration. Spec: INTEGRATIONS.md §7 (tax summary row + QBO summary-slot constraint). **Also reworked the Order Details totals boxes into a full, always-on itemized breakdown** (`app.orders.$id.jsx`) per the requested display rules — **Sales tax, Shipping charges, and Discount always render (even at $0.00)**, the **Processing Fee shows as its own clearly-described row** ("Credit Card Processing Fee – 3% of $596.58" — % + calculation basis), and an **"Other charges"** reconciling row appears only when the grand total carries an amount the named rows don't explain (so the breakdown can never silently hide a charge). Applied to BOTH the Shopify "Line items" box (`invoiceCalc` from `computeInvoiceCalculation`) and the QBO "QuickBooks invoice" panel (`projectQboInvoice`): the QBO panel's line table now lists **products only** (`productLines`), with Shipping / Discount / Processing fee / Tax surfaced as their own summary rows (Subtotal = products only → Discount → Shipping → Sales tax → Processing fee → Other charges → Grand total → Balance due). `projectQboInvoice` classifies `SalesItemLineDetail` rows by description (`/^shipping$/i`, `/processing fee/i`) into `productLines` + `productSubtotal` + `shipping` + `processingFee`(+`Label`, the verbatim self-documenting QBO description) and reads the `DiscountLineDetail` amount into `discount` (previously dropped, so a discounted invoice's total looked unexplained — e.g. #1142's $646.20 products − $64.62 discount + $15.00 shipping + $17.90 fee = $614.48). The fee description is enriched at the source too: `buildProcessingFeeLine` gained an optional `baseAmount` param, and `createInvoiceForOrder` passes `feeBase`, so the **customer's QBO invoice line** also reads "… – 3% of $596.58". **QBO constraint reminder:** these always-on / products-only-subtotal rules apply to the app's own panels (full control); the QBO-*rendered* customer invoice still can't show $0 tax/discount rows or move shipping/fee to its summary (only the enriched fee-line description carries over).

- **2026-06-05** — Made the invoice show its **full pricing breakdown** — including the **processing fee as a line item** — up front, so the customer understands the final amount that will be charged. Three changes: **(1) Processing fee at invoice creation.** `createInvoiceForOrder` now computes the per-method fee (card 3% / ACH 1% / cheque 0%, `invoiceConfig.processingFeeRates`) on the order's post-discount grand total (`order.total_price`) and pushes the `<Method> Processing Fee – X%` line onto the QBO invoice **at creation** for card/ACH, stamping `processingFeeAmount/Rate/Method` + `processingFeeAppliedAt`. `amountDue` (from QBO `TotalAmt`) and the initial customer invoice email are therefore fee-inclusive. Every settlement-time fee path already guards on `!processingFeeAppliedAt`, so the fee is never double-added; the settlement append (`propagateSuccessfulPayment` §0) is now the **fallback** for the cheque→card admin override and legacy invoices. Cheque (0%) gets no fee line and leaves `processingFeeAppliedAt` unset, preserving the existing fallback behavior. **(2) QBO discount line.** `shopifyLinesToQboLines` now emits a `DiscountLineDetail` line (`{ kind: 'discount' }` → `qbo.utils.toInvoiceLine`) when `order.total_discounts > 0`, so the QBO total reconciles with Shopify's post-discount `total_price` (previously discounts were ignored and the invoice over-stated). **(3) UI breakdown.** Order Details totals box renders the full flow — Order subtotal → Discount → Adjusted subtotal → Shipping → Tax → Payment processing fee → Grand total — via a shared pure helper `computeInvoiceCalculation` (`invoice.utils.js`); `extractBreakdown` gained `lineItemsTotal` (gross). The Orders list "Amount" column shows the fee-inclusive `Invoice.amountDue` (with the pre-fee Shopify total as a subdued subtitle when they differ). No NMI/ACH-settlement/idempotency changes; no data migration (legacy fee-at-settlement invoices keep working via the unchanged guards). Spec: INTEGRATIONS.md §7 (processing-fee + discount line).

- **2026-06-03** — Added three **preferred-payment-method filter chips** to the Order List (`app.orders._index.jsx`): **Card**, **ACH**, **Cheque**. Invoice-scoped chips (ids `pref_card` / `pref_ach` / `pref_cheque`) that filter on the immutable order-time `Invoice.customerPaymentPreference` snapshot — NOT the mutable `paymentMethod` (which flips when an admin uses the cheque→card fallback) — so the chip reflects what the customer actually chose at order time. Each falls back to `paymentMethod` for legacy invoices created before the preference snapshot existed (mirrors the existing `dueAt`→`qboDueDate` fallback). Implemented purely by extending `STATUS_FILTERS` + `buildInvoiceFilter`; the existing invoice-scoped chip rendering, count-query, search, and pagination paths handle them generically with no further changes. Distinct from the existing payment-status "Pending cheque" chip (which gates on `paymentStatus` + method). No model, API, or scheduler changes.

- **2026-06-03** — Added an on-demand **"Sync ACH status"** button to the Order Details page so admins can reconcile a single ACH transaction with NMI without waiting for the scheduled `process-ach-status-sync` CRON. The per-invoice reconciliation body was **extracted from the CRON into a shared `reconcileAchInvoice`** (`services/payment/achStatusSync.service.js`) used verbatim by BOTH the CRON sweep (`syncAchTransactionStatuses`) and the new manual path — NMI lookup → `payment.service.checkAchSettlement` (still the single source of truth for the `awaiting_settlement → paid/pending/failed` transition) → `achStatusHistory[]` audit row + `cron_ach_settlement_check` remark + return-code capture + critical-alert push. New service fn `manualSyncAchInvoice` takes an **atomic per-invoice lock** (`Invoice.findOneAndUpdate({ achSyncInProgress: { $ne: true } }, …)`, released in `finally`) so a double-click or an overlapping CRON tick can never reconcile the same invoice twice (blocked request → 409 "already in progress"). New endpoint `POST /api/admin/orders/:id/sync-ach-status` (`api/admin/sync-ach-status.js`, registered in `routes.js`) — ACH-only, requires `awaiting_settlement` + `pendingSettlementTxnId`, else 409. New Invoice fields: `achSyncInProgress` (lock) + `achSyncStartedAt`, and the display fields `achSyncLastAt` / `achSyncLastStatus` (normalized: settled/returned/voided/failed/pending_settlement/unknown/error) / `achSyncLastCondition` (raw NMI condition) / `achSyncLastSource` (`cron_ach_status_sync` | `admin_manual_sync`) / `achSyncLastBy` — written on EVERY sync (CRON or manual, any outcome) so the page can always show "Last ACH sync: <time> · <status badge> · via manual/scheduled sync". UI: button shown only for ACH invoices awaiting settlement (disabled while a sync is in flight via the fetcher state OR `achSyncInProgress`), a dedicated result banner (settled → success, returned → critical, still-pending → info), and the last-sync line under the existing "ACH awaiting settlement" banner. A manual sync bypasses the once-per-day "still settling" remark throttle. No change to the CRON cadence, the settlement money/state logic, or any other admin action. Spec: INTEGRATIONS.md §9.5.1 (on-demand sync subsection).

- **2026-06-03** — Made the **`process-ach-status-sync` CRON cadence environment-configurable** and aligned the production default with the intended schedule. The mechanism already existed (`ACH_SYNC_INTERVAL` overrides `ACH_SYNC_CRON` when set), so this is config + default + docs only — no logic change. **Testing:** `ACH_SYNC_INTERVAL=1 minute` (currently active in `.env`) runs the sweep every minute for rapid validation of ACH status updates / reconciliation. **Production:** comment that line out and the job falls back to `ACH_SYNC_CRON`, whose default changed from every 6 hours (`0 */6 * * *`) to **once per day at 03:00 (`0 3 * * *`)** — ACH settles in 1–3 business days, so a single daily reconciliation pass is plenty. Both knobs are fully overridable via env vars without a code change. Updated: `achStatusSync.config.js` default + comments, `processAchStatusSync.job.js` + `scheduler.service.js` cadence comments, `.env` block, INTEGRATIONS.md §9.5.1 prose + env table. No change to the sweep logic, settlement transitions, audit trail, or alerting.

- **2026-06-02** — Added a dedicated **ACH Status Synchronization CRON** (`process-ach-status-sync`), independent of the payment-processing CRON, whose sole job is reconciling in-flight ACH transactions with NMI. **Extracted the former "PASS 1.7"** settlement check out of `process-pending-payments` into this job so there is a single owner of the `awaiting_settlement → paid/pending/failed` transition (no race between two CRONs) and so settlement is polled on its own frequent cadence (default every 6h via `ACH_SYNC_CRON`; dev `ACH_SYNC_INTERVAL`) rather than only on the twice-monthly charge ticks. New files: `services/payment/achStatusSync.service.js` (`syncAchTransactionStatuses` — cursor `{ paymentMethod:'ach', paymentStatus:'awaiting_settlement', pendingSettlementTxnId set }`, i.e. non-final only; per-invoice try/catch so one failure never stops the sweep; idempotent — settled/returned invoices leave the cursor and a still-pending poll writes nothing), `services/payment/achStatusSync.config.js`, `services/scheduler/jobs/processAchStatusSync.job.js` (thin wrapper, `concurrency:1`); registered in `jobs/index.js` + `scheduler.service.ensureRecurring` (before the retry dev-override early-return so it runs in dev too). The money/state transition stays in `payment.service.checkAchSettlement` (single source of truth, shared with admin actions); it was enhanced to persist `achReturnCode`/`achReturnReason`/`achReturnedAt` on a return and to return `returnCode`/`newStatus`. New Invoice fields: `achReturnCode`, `achReturnReason`, `achReturnedAt`, and `achStatusHistory[]` (append-only audit trail — one entry per detected status CHANGE: `status` ∈ pending_settlement/settled/returned/voided/failed/unknown, `previousStatus`, `nmiCondition`, `nmiTransactionId`, `returnCode`, `returnReason`, `amount`). Operator remarks reuse the existing `cron_ach_settlement_check` kind (no UI badge-map change). Admin notification on critical returns/voids/stuck (> `ACH_SYNC_STUCK_DAYS`, default 5): high-visibility `log.error('ach.alert', …)` + console banner + optional outbound webhook gated by `ACH_ALERT_WEBHOOK_URL` (off by default — never POSTs payment data externally unless configured). `process-pending-payments` now only charges (PASS 1) / logs failed follow-ups (PASS 1.5) / retries downstream sync (PASS 2); its tick summary dropped the settlement counters. `.env` activates testing mode (`ACH_SYNC_INTERVAL=1 minute`). Spec: INTEGRATIONS.md §9.5.1 (reconciliation CRON + audit/return capture) + env table.

- **2026-06-02** — Added [ACH_Feasibility_Analysis.md](ACH_Feasibility_Analysis.md) — a client-facing "ACH Payment Automation – Feasibility Analysis and Implementation Approach" document. Nine sections: Executive Summary, Current State (existing payment workflow, NMI integration status, available ACH capabilities), Proposed ACH Automation Flow (enrollment → closure lifecycle, ASCII + mermaid), NMI ACH APIs with masked sample payloads/responses (vault creation, add-billing, ACH sale, status lookup, settlement-via-polling note), ACH Automation Process, Settlement & Return Handling (timelines, pending-settlement, return codes R01–R29 + invoice/customer impact), Risks/Dependencies/Blockers, phased Implementation Plan with T-shirt sizing, and Conclusion (assumptions + areas requiring client confirmation). ACH-only. Cross-references `INTEGRATIONS.md`. Documentation only.

- **2026-06-02** — Removed cheque-reminder logging from the payment CRON (`process-pending-payments` PASS 1.5) to eliminate duplicate reminders and enforce a clean separation of responsibilities. Symptom: the Order Details remarks ledger showed both `cron_payment_reminder` rows (from the dedicated `process-check-reminders` CRON, which sends the real QBO emails) **and** `cron_cheque_reminder` "Cheque payment reminder — $X still outstanding" rows (from `process-pending-payments` PASS 1.5, log-only) on the same unpaid cheque invoice. PASS 1.5 now runs only over `paymentStatus: 'failed'` invoices and logs `cron_failed_followup` as part of the payment CRON's audit history — its cursor's `{ paymentStatus: 'pending', paymentMethod: 'check' }` branch was dropped, so it no longer emits `cron_cheque_reminder`. Customer-facing cheque reminders are now owned **exclusively** by `process-check-reminders` (services/reminder). `process-pending-payments` is responsible only for charging (card/ACH), status updates, and payment/audit logs. The legacy `cron_cheque_reminder` / `cron_ach_reminder` remark-kind enum values stay on the Invoice schema for back-compat with historical rows (the Order List/Details badge map still renders them) but are no longer written. Tick summary counter renamed `remindersLogged` → `followupsLogged`. No change to the reminder CRON, payment charging, or any admin action. Spec: INTEGRATIONS.md §11.1 (PASS 1.5) + flow diagram.

- **2026-06-02** — Added a **recurring reminder phase** to the Check-payment reminder CRON: after the final ladder stage (`card` / Day 13) is sent and the invoice is still unpaid, the job keeps emailing a `recurring` reminder until the balance is paid, instead of going silent after Day 13. Cadence is `REMINDER_REPEAT_DAYS` (prod, default 2) / `REMINDER_REPEAT_MINUTES` (test, default 1), chosen by `REMINDER_USE_MINUTES`. Implementation: new `recurringStage` descriptor + `recurringIntervalUnits()` resolver in `reminder.config.js`; `reminder.service.dueStage` now takes `now` and, once the highest-threshold named stage is already sent, calls `recurringDue()` — which throttles on the most recent `paymentReminders[]` `sentAt` so the email cadence is independent of how often the CRON ticks (and never fires twice within one cycle). New `recurring` enum value on `Invoice.paymentReminders[].stage` (its rows accumulate, one per cycle, as the audit trail; the named stages still fire at most once each). Reminders still stop automatically the moment the invoice is paid (it leaves the eligibility filter) or is paused (`reminderPaused`). No payment-processing, due-date, or pause-control behavior changed. Spec: INTEGRATIONS.md §10.5 (recurring phase + ladder table), env table.

- **2026-06-02** — Three related changes to the Check-payment reminder + invoice-due-date flow. **(1) Reminder ladder first stage moved Day 7 → Day 9** — the production ladder is now **Day 9 / Day 11 / Day 13** (`first` / `second` / `card`), env `REMINDER_DAY_FIRST` default 9. Testing minute ladder (1 / 3 / 4) unchanged. **(2) Per-invoice "Pause auto email notifications" control.** New `Invoice.reminderPaused` Boolean (+ `reminderPausedAt` / `reminderPausedBy` / `reminderPauseNote` / `reminderResumeAt` / `reminderResumedBy`) mutes the reminder CRON for one invoice without affecting any other invoice or the card auto-charge sweep. The reminder eligibility filter now gates on `reminderPaused: { $ne: true }` (replacing the previously-misapplied `autoChargePaused` clause — that flag is card-only and the reminder CRON is cheque-only, so it was effectively a no-op there). Two new admin endpoints `POST /api/admin/orders/:id/pause-reminders` + `/resume-reminders` (mirror the auto-charge pause endpoints; gated to cheque invoices; optional `{ note }` → `remarks[]`; idempotent). Order Details UI gains a Pause/Resume button (cheque invoices only), an "Email reminders active/paused" badge, a paused-state context banner, and confirmation modals — all mirroring the existing auto-charge pause UI. Reminders also stop automatically once an invoice is paid (it leaves the `paymentStatus ∈ {pending, partially_paid}` set). **(3) Per-payment-method invoice due dates.** `DueDate` (QBO) + local `dueAt` are now computed from the invoice's locked `paymentMethod` instead of a single `INVOICE_TERMS_DAYS`: cheque → `CHEQUE_DUE_DATE`, ACH → `ACH_DUE_DATE`, card → `CARD_DUE_DATE` (days from order date; each falls back to `INVOICE_TERMS_DAYS` when unset). New `invoiceConfig.dueDaysByMethod` + `dueDaysForMethod(method)` resolver in `services/invoice/invoice.config.js`; `createInvoiceForOrder` calls `dueDaysForMethod(invoice.paymentMethod)`. `.env` seeded with `CHEQUE_DUE_DATE=15` / `ACH_DUE_DATE=12` / `CARD_DUE_DATE=10` (the spec example values; adjustable). Boot banner prints the per-method terms. No payment-processing or scheduler-PASS behavior changed. Spec: INTEGRATIONS.md §7.3 (per-method due dates), §10.5 (ladder + pause control), env table, Collections table.

- **2026-06-02** — Reworked the **Check-payment reminder CRON** into an explicit two-ladder design and switched it into a one-minute testing cadence. (1) The reminder ladder now has a distinct **production ladder in days (First = 7 / Second = 11 / Card-on-file = 13)** and **testing ladder in minutes (First = 1 / Second = 3 / Card-on-file = 4)** — previously `REMINDER_USE_MINUTES` just reinterpreted the single 7/9/13 set as minutes, which could not produce the requested 1/3/4 testing schedule, and the middle production threshold was Day 9 rather than the requested Day 11. `reminder.config.js` now exposes both ladders (`dayFirst/daySecond/dayCard` + `minFirst/minSecond/minCard`, env `REMINDER_DAY_FIRST/SECOND/CARD` + `REMINDER_MIN_FIRST/SECOND/CARD`) and `reminderStages()` picks the active threshold per stage by `useMinutes`. (2) Stage keys renamed from threshold-coupled `day7/day9/day13` to semantic `first/second/card` so the dedup history (`Invoice.paymentReminders[].stage`) stays stable across the day↔minute switch; the `stage` enum keeps the legacy `day7/day9/day13` values for back-compat with any pre-existing rows. (3) The middle stage label changed "Final payment reminder" → **"Second payment reminder"**; the last stage label is now **"Final card-on-file notice"**. (4) `.env` activates TESTING mode: `REMINDER_INTERVAL=1 minute` + `REMINDER_USE_MINUTES=true` so the sweep runs every minute and the full ladder fires in ~4 minutes; reverting to production is commenting out those two lines (falls back to the daily cron + day ladder). The card-processing retry CRON (`PAYMENT_RETRY_INTERVAL=5 minutes`, card-only PASS 1) is untouched and runs independently — Check invoices are never auto-charged, only reminded. No payment-processing, eligibility-filter, dedup/audit (`paymentReminders[]` / `emailEvents[].source:'payment_reminder'` / `remarks[].kind:'cron_payment_reminder'`), or admin-endpoint behavior changed. Spec: INTEGRATIONS.md §10.5 (ladder table + env block), CLAUDE.md status row "Check-payment reminder CRON".

- **2026-06-01** — Added a standalone daily **Check-payment reminder CRON** (`process-check-reminders`), separate from the payment-retry ticks. Runs once a day (default cron `0 2 * * *`, env `REMINDER_CRON`; dev override `REMINDER_INTERVAL`) and emails QBO invoice reminders on a Day 7 / Day 9 / Day 13 ladder for unpaid **Check** invoices only. Responsibilities are strictly limited to *notification* — it identifies eligible invoices (`paymentMethod: 'check'`, `paymentStatus ∈ {pending, partially_paid}`, `qboCreationStatus: 'created'`, has `qboInvoiceId`, `autoChargePaused ≠ true`), determines the due stage, triggers the QBO invoice email via the existing `qbo.service.sendInvoiceEmail` (`/invoice/<id>/send`), and records history; it **never charges a payment method** (the Day 13 notice only tells the customer the balance may be charged to the card on file — any such charge is an admin action). Elapsed time is measured from the order/issue date (`qboTxnDate`, fallback `createdAt`). Logic lives in `services/reminder/reminder.service.processCheckPaymentReminders()` + `reminder.config.js`; the job is a thin Agenda wrapper in `services/scheduler/jobs/processCheckReminders.job.js`, registered in `jobs/index.js` and scheduled in `scheduler.service.ensureRecurring` (registered in dev + prod). Dedup + audit: new `Invoice.paymentReminders[]` history (one entry per stage, `status: sent|failed`) is the source of truth that prevents re-sending a stage — only a `sent` entry suppresses a stage; a `failed` entry is retried next run. Each send also appends an `emailEvents[]` row (new `source: 'payment_reminder'`) and a `remarks[]` entry (new `kind: 'cron_payment_reminder'`, distinct from the legacy log-only PASS 1.5 `cron_cheque_reminder`). Reminder ladder is "current level wins" — each run sends only the highest-threshold stage reached and not yet sent, so a CRON outage jumps straight to the most-advanced reminder rather than replaying earlier ones; each stage fires at most once, in order. Testing knob `REMINDER_USE_MINUTES=true` interprets the day thresholds as minutes (mirrors `INVOICE_TERMS_MINUTES`) so the ladder can be exercised without waiting days. New `Invoice` fields: `paymentReminders[]`; new enum values: `remarks[].kind: 'cron_payment_reminder'`, `emailEvents[].source: 'payment_reminder'`. No payment-processing, scheduler-PASS, or admin-endpoint behavior changed. Spec: INTEGRATIONS.md §10.5 (new — Check reminder CRON).

- **2026-05-29** — Fixed ACH invoices being marked Paid the moment NMI returned response code 100, even though the ACH transaction was still in `pendingsettlement` condition (1–3 business days before the bank confirms or returns the debit). Root cause: card and ACH were sharing the same approval-side code path in `payment.service.chargeInvoice` — both bumped `amountPaid`, fired `propagateSuccessfulPayment` (which writes QBO `Payment` records and Shopify `markOrderPaid`), and let `applyDerivedPaymentStatus` settle the status to `paid`. Card sales are funds-captured at NMI's approval so that's correct, but ACH approval just means "submitted to the network" — a later return would have left the local Invoice, QBO, and Shopify all falsely claiming paid. Fix is a four-piece change: (1) new `'awaiting_settlement'` enum value on `Invoice.paymentStatus`, made **sticky** in `deriveInvoicePaymentStatus` (like `cancelled`) so a stray re-derive can't bounce it back to `pending` and double-submit; (2) new in-flight fields on Invoice (`pendingSettlementTxnId` / `Amount` / `FeeAmount` / `Since` / `LastCheckedAt`) carry the submitted amount and the NMI transaction id without touching `amountPaid`; (3) `chargeInvoice` ACH branch now sets `awaiting_settlement` + persists the in-flight fields instead of bumping `amountPaid` / running propagate; (4) new scheduler **PASS 1.7** calls `payment.service.checkAchSettlement` for every awaiting-settlement invoice, which queries NMI's `query.php?transaction_id=<id>` (new helper `getNmiTransactionStatus`) and acts on the `condition`: `complete` → apply the in-flight credit + propagate to QBO/Shopify (status → `paid`), `failed`/`canceled` → drop the credit + write a new `declined`/`error` PaymentAttempt + bump attemptCount (status → `pending` for retry, or `failed` if maxAttempts), `pendingsettlement`/`pending` → leave alone. New `cron_ach_settlement_check` remark kind ("still pending" entries throttled to once per day so the panel doesn't flood during the normal wait window). Admin endpoints (`retry-payment`, `charge-card`) now hard-reject 409 while the invoice is in `awaiting_settlement` to prevent double-charging the customer if the ACH eventually settles. UI: `PaymentStatusBadge` renders "Awaiting settlement" (info tone); Order Details pipeline subtitle shows "ACH submitted — awaiting settlement"; an info banner on the page explains the 1–3 day window with the in-flight transaction id and amount. Card behavior is unchanged. Spec: INTEGRATIONS.md §9.5.1 (new — ACH settlement lifecycle), CLAUDE.md status table row for "Scheduler PASS 1.7".

- **2026-05-29** — Fixed ACH charge failing with `NMI ACH vault <id> invalid: vault id not present in NMI response`. Root cause: the previous day's "ACH auto-charge" implementation mis-modelled the architecture as "two separate NMI customer vaults per customer — one for card at `nmiCustomerVaultId`, one for ACH at `payment.ach.nmi_billing_id`". That's wrong. NMI uses ONE customer vault per customer; `billing_id` is the id of a SPECIFIC billing profile (card vs ACH) INSIDE that vault. The fix:  (1) `nmi.service.chargeCustomerVault` now accepts an optional `billingId` and threads it into transact.php's `billing_id` parameter alongside `customer_vault_id`; (2) `payment.service.resolveInvoiceVault` now returns `{ vaultId, billingId }` — vault always from `customerMap.nmiCustomerVaultId` (mirrored from `wholesale_applications.nmiCustomerVaultId`), billing id only when ACH (mirrored from `wholesale_applications.payment.ach.nmi_billing_id`); (3) `validateCustomerVault` runs against the vault id ONLY (not the billing id — NMI's `query.php?report_type=customer_vault` queries vaults, not billings, so the stale call was guaranteed to fail with "vault id not present in NMI response"); (4) `customer.service.ensureCustomerForOrder` just mirrors the ACH billing id onto CustomerMap without validating it; (5) ACH retry / charge paths require BOTH ids and surface precise 409s when either is missing; (6) `registration-form.js` reverted to always storing the created vault id at top-level `nmiCustomerVaultId` (regardless of method) — the ACH billing id is populated separately by an admin tool / NMI dashboard / future add_billing call. Spec: INTEGRATIONS.md §6.4 (rewritten — vault + billing-id, not two vaults), §9.5 / §9.2 (updated for the correct call shape).

- **2026-05-29** — ACH auto-charge is now a first-class CRON path alongside card. ACH-preferred customers used to sit in PASS 1.5 as "ACH reminder" rows that an admin had to settle manually; they now go through PASS 1 the same way card invoices do. New customer-level field `wholesale_applications.payment.ach.nmi_billing_id` (snake-case per the domain spec) stores the dedicated NMI customer-vault id created at registration when `payment.method === 'ach'` — distinct from the top-level `nmiCustomerVaultId`, which continues to hold the card vault. `CustomerMap` gains a parallel `nmiAchBillingId` cache field, populated at order intake by `customer.service.ensureCustomerForOrder` (validated via `validateCustomerVault` just like the card path) and exposed via a new `resolveCustomerAchBillingId` helper that mirrors `resolveCustomerVaultId`'s cache → source-of-truth fallback chain. `payment.service.chargeInvoice` now calls `resolveInvoiceVault(invoice, customerMap)` to pick the right vault id by `invoice.paymentMethod` ('card' → `nmiCustomerVaultId`, 'ach' → `nmiAchBillingId`); the NMI sale itself is method-agnostic since NMI's vault stores the payment type. CRON PASS 1's cursor expanded to `paymentMethod: { $in: ['card', 'ach'] }`; PASS 1.5 trimmed to cheque-pending + failed invoices only (failed ACH gets `cron_failed_followup`, same as failed card). New `cron_ach_attempt` enum value on `Invoice.remarks[].kind` distinguishes ACH charges from card charges in the Order Details remarks panel; legacy `cron_ach_reminder` is preserved on the schema for back-compat with historical rows. Admin endpoints: `POST /api/admin/orders/:id/retry-payment` now accepts ACH invoices (routes to the ACH billing id; previously rejected as "use mark-cheque-paid"); `POST /api/admin/orders/:id/charge-card` was already method-agnostic for the cross-method fallback (flips `Invoice.paymentMethod` to 'card', charges the card vault) — the only change is that it now also covers the ACH-failure → card-on-file flow described in the spec. Order Details UI: ACH-pending invoices now show a "Retry ACH payment" primary button + the existing "Charge card on file" secondary button (cross-method fallback). The "no auto-charge" subdued note for cheque/ACH invoices is now method-aware — ACH invoices read "scheduler auto-charges the ACH billing profile … if ACH fails, fall back to charging the card on file". Spec: INTEGRATIONS.md §6.4 (rewritten — per-method vault sourcing), §9.1 / §9.2 / §9.5 (updated for ACH auto-charge), CLAUDE.md status table rows for "NMI vault add + sale" and "Scheduler PASS 1".

- **2026-05-28** — Fixed `ReferenceError: process is not defined` crashing the NMI Dashboard / Customers / Payments / Transactions / Failed / Refunds tabs in the browser. Root cause: client render code (e.g. `app.nmi.dashboard.jsx`) imported `latestAction` + `fromNmiDate` from `services/nmi/nmi.service.js`. React Router 7's Vite plugin strips loader-only imports from the client bundle, but those two helpers are used in **render** code, so Vite kept `nmi.service.js` in the client bundle. The whole transitive chain came along — `nmi.service.js` → `nmi.apis.js` → `nmi.config.js`, which calls `readEnv()` at module init and references `process.env`. In the browser that's `undefined` → crash. QBO didn't repro because the QBO dashboard's render code uses zero exports from `qbo.service.js` (only `formatAmount` / `fmtDueDate` from `format.utils.js`); the only `qbo.service.js` import is `getDashboardSnapshot`, which is loader-only and gets stripped. Fix: moved the pure render-time helpers (`toNmiDate`, `fromNmiDate`, `latestAction`) and the XML parsers (`parseNmiTransactions`, `parseNmiCustomerVaults` + their internal extractors) from `nmi.service.js` to `nmi.utils.js` (which only imports from `nmi.constants.js` — no config dependency). Updated all 6 NMI route files to import these helpers from `nmi.utils.js`; `nmi.service.js` re-imports them internally where needed. Project rule going forward: **client render code must never import from `<svc>.service.js`** — pure helpers belong in `<svc>.utils.js`, server-only fetch/orchestrator functions in `<svc>.service.js`. The same trap exists for `qbo.service.js` (it imports `qbo.config.js` with the same `readEnv` chain) — currently masked only because nothing in render imports from it.

- **2026-05-28** — Fixed clicking the QBO / NMI nav items bouncing the user back to the app's home page (the "Apps > dev-rk" landing) instead of opening the section. Root cause: both `app.qbo._index.jsx` and `app.nmi._index.jsx` used react-router's plain `redirect()` to bounce `/app/qbo` → `/app/qbo/dashboard` (and the NMI equivalent). Inside the embedded Shopify admin iframe, a plain `Location:`-header redirect is intercepted by App Bridge and rebounded to the app's home — the "embedded-app navigation" rule in CLAUDE.md and README says use the `redirect` returned by `authenticate.admin()` instead, which renders an App Bridge HTML response that performs the navigation within the iframe. Fix: both index loaders now do `const { redirect } = await authenticate.admin(request); return redirect('/app/<section>/dashboard');`. QBO had the same latent bug — users hitting `/app/qbo/dashboard` directly via a deep link wouldn't have noticed, but the menu click would have repro'd. See `node_modules/@shopify/shopify-app-react-router/src/server/authenticate/admin/helpers/redirect.ts` for the iframe vs non-iframe detection (it `throws renderAppBridge(...)` for embedded requests).

- **2026-05-28** — Added top-level **NMI** admin section. New nav item between QBO and Additional page. Six tabs: **Dashboard** (counts + recent transactions, scoped to a 7/30/90-day window), **Customers** (NMI Customer Vault entries, filtered by card / ACH / all + free-text search across name/email/phone/vault id), **Payments** (sale + capture + credit actions with method/period chips), **Transactions** (all transaction activity with condition chip — complete / awaiting settlement / failed / canceled), **Failed payments** (declined or errored transactions with retry-count badge + "latest action vs any action" toggle), **Refunds** (one row per refund action, so partial-refund lifecycles expand correctly — paired with the original sale's date / amount for cross-reference). All data pulled LIVE from NMI's `query.php` (XML), parsed via a small regex-based block extractor in `services/nmi/nmi.service.js` (no XML parser dependency — NMI's response shape is shallow + well-known). New service exports: `listNmiTransactions`, `listNmiCustomerVaults`, `getNmiDashboardSnapshot`, `parseNmiTransactions`, `parseNmiCustomerVaults`, `latestAction`, `toNmiDate` / `fromNmiDate`. Date filter mandatory (defaults to last 30 days) because NMI's `query.php` has no pagination tokens — bounding the window keeps the response size manageable. Each list loader fetches the window once, applies search filters in JS, and paginates client-side at 50 rows/page (the parsed-array sort is newest-first by latest-action date). Dashboard fans out 2 parallel NMI calls + computes counts in JS (sale/capture/credit → successful, refund → refund pool, condition='failed' OR success!=1 → failed, transaction_type ck → ACH, cc → card). Per-metric failures degrade to `null` via a local `safe()` wrapper, surfaced as a warning banner. Spec: INTEGRATIONS.md §8 (NMI read-only list helpers).

- **2026-05-28** — Added top-level **QBO** admin section. New nav item between Orders and Additional page (per request). Four tabs: **Dashboard** (live counts, monthly revenue snapshot, recent payments + invoices), **Customers** (paginated QBO customer list with status filter + name/email/company search), **Transactions** (paginated QBO Payment list with date-range chips + reference search), **Invoices** (paginated QBO Invoice list with payment-status filter + invoice-number search). All four tabs are independent React Router 7 child routes under a layout (`app/routes/app.qbo.jsx`) so per-tab failures don't cross-contaminate and URL search params (filter / search / page) survive deep links + browser back. Data is pulled LIVE from QBO via QL `/query` — no DB caching, no scheduled syncs. Each list route runs the page query + COUNT query in parallel (QBO QL doesn't return totals for paginated lists). New QBO service helpers in `services/qbo/qbo.service.js`: `listCustomers` / `countCustomers` / `listInvoices` / `countInvoices` / `listPayments` / `countPayments` / `getDashboardSnapshot`. Shared internal helpers `runListQuery` + `runCountQuery` keep the QBO QL plumbing in one place — clamped to MAX_PAGE_SIZE=200, defaults DEFAULT_PAGE_SIZE=50. `getDashboardSnapshot` is composite: 9 parallel queries with per-metric `safe()` wrapping so one failure becomes a `null` entry on the response's `errors[]` array (surfaced as a warning banner) rather than failing the whole call. Per-tab Refresh buttons call `useRevalidator().revalidate()` so the live data updates without a full page navigation. Dashboard tile click-through navigates to the Invoices tab with the right filter pre-applied (e.g. clicking "Pending invoices" lands on `/app/qbo/invoices?filter=pending`). Voided-invoice handling: paid-count predicate adds `TotalAmt > '0'` so voided rows (Balance=0 + TotalAmt=0) don't inflate the "Paid invoices" tile. Voided invoices get their own filter chip + badge tone on the Invoices tab. Spec: INTEGRATIONS.md §7 (QBO read-only list helpers).

- **2026-05-28** — Fixed CRON reminder remarks for ACH-method invoices rendering with a "Cheque reminder" badge on Order Details. Root cause: `processPendingPayments.job.js` PASS 1.5 used a single `cron_cheque_reminder` kind for both cheque AND ACH invoices — the message text picked the right "ACH"/"Cheque" word but the badge in the Order Details Remarks table is keyed off the kind alone via the `REMARK_KIND_META` map, so ACH invoices got "Cheque reminder". Fix: new `cron_ach_reminder` enum value on `Invoice.remarks[].kind`; CRON now picks the kind by `invoice.paymentMethod` (failed-card → `cron_failed_followup`, ACH-pending → `cron_ach_reminder`, anything else pending-manual → `cron_cheque_reminder`); `REMARK_KIND_META` gets an "ACH reminder" warning-tone entry. The Order List `RemarksCell` was already method-aware via `invoice.paymentMethod` so it needed no changes. Historical remarks logged before this fix keep their (now mis-labeled) `cron_cheque_reminder` kind — no backfill — but their `message` text still correctly reads "ACH payment reminder — …" so the row is unambiguous; the next CRON tick on each affected invoice appends a freshly-tagged ACH entry.

- **2026-05-28** — Per-invoice auto-charge pause/resume control on Order Details. Admins can take an individual card-preferred invoice out of the CRON auto-charge sweep without affecting any other invoice, the customer's broader payment preference, or the stored vault. New Invoice fields: `autoChargePaused` (Boolean, indexed), `autoChargePausedAt`, `autoChargePausedBy`, `autoChargePauseNote`, `autoChargeResumeAt`, `autoChargeResumedBy`. Two new admin endpoints — `POST /api/admin/orders/:id/pause-auto-charge` and `POST /api/admin/orders/:id/resume-auto-charge` — both accept an optional `{ note }` body that is mirrored into the existing `Invoice.remarks[]` audit ledger (`kind: 'admin_action'`, `source: 'admin'`). CRON scheduler (`services/scheduler/jobs/processPendingPayments.job.js`) gains `autoChargePaused: { $ne: true }` to both the PASS 1 (card-charge) cursor and the PASS 1.5 (cheque/ACH + failed-card reminder) cursor — paused invoices are excluded from both sweeps. PASS 2 sync-retry is unaffected (it only replays downstream sync for invoices that already have money on them, which is orthogonal to whether new charges are allowed). UI: secondary-action button in the page header (Pause when active / Resume when paused — one shows at a time), a status badge in the Invoice & payment section, a warning banner that surfaces who/when/why when paused, and KV rows ("Auto-charge status", "Paused at" / "Last resumed"). Feature gated to `customerPaymentPreference === 'card'` — the immutable order-time snapshot, not the mutable `paymentMethod`, so a cheque-preferred customer whose invoice was overridden to card via the existing fallback does NOT see the pause UI (their preference hasn't changed). Server-side guard re-checks the same condition + rejects pause attempts on `paid`/`cancelled`/`in_progress` invoices. Resuming does NOT trigger an immediate charge — it just unblocks the next CRON tick; the existing Retry payment button stays the path for "charge now". Endpoints are idempotent: re-pausing refreshes timestamps, re-resuming records a "was already running" confirmation in remarks. Spec: INTEGRATIONS.md §9 (auto-charge pause control) + §17 (Invoice schema).

- **2026-05-26** — Added `Invoice.emailEvents[]` append-only audit ledger + "Email history" section on the Order Details page. Captures every QBO `/invoice/<id>/send` attempt — successes AND failures — so admins can verify the customer received the latest invoice and trace any failed deliveries. Per-row fields: `createdAt`, `triggerType` (`'auto'` for lifecycle-dispatcher sends, `'manual'` for admin "Send invoice" button), `triggeredBy` (`'system'` or admin email), `source` (`invoice_created` / `payment_recorded` / `status_changed` / `manual_resend`), `recipient`, `status` (`'sent'` / `'failed'`), `errorMessage`, and `paymentStatusSnapshot` / `amountPaidSnapshot` (so the row reads sensibly even after later payments change the live invoice state). Single shared writer `recordEmailEvent(invoice, eventData)` exported from `invoice.service.js`; called by `dispatchInvoiceLifecycleEmails` (both success and failure branches) and `api/admin/send-invoice.js` (manual path) before each side's own `.save()`, so the ledger and the dedup-baseline fields always persist atomically in one Mongo write. UI renders as a Polaris table with success/failure badges, sorted newest-first. Spec: INTEGRATIONS.md §7.7 (extended with audit-ledger subsection — was §7.6 before the 2026-05-28 QBO admin section moved it).

- **2026-05-26** — Added "Send invoice" button to the QuickBooks invoice section of the Order Details page. Routes through new `POST /api/admin/orders/:id/send-invoice` → `qbo.service.sendInvoiceEmail`, then advances the email-tracking baseline (`invoiceEmailSentAt`, `invoiceEmailLastSentAt`, `invoiceEmailedStatus`, `invoiceEmailedAmountPaid`) so the auto-dispatcher's amount-grew / status-changed re-send rule doesn't immediately fire a duplicate on the next payment event. Appends an `admin_action` remark with the admin's session email + recipient for the Order List "Remarks" column. Returns 502 + persists `lastEmailError` on QBO failure so the banner surfaces the message. React Router auto-revalidates the loader after the fetcher action, so the live QBO `EmailStatus` / `Last updated` fields refresh without a page reload.

- **2026-05-26** — Customer-facing invoice emails now flow through QBO's built-in mailer. New `sendInvoiceEmail` in `services/qbo/qbo.service.js` wraps `POST /v3/company/{realmId}/invoice/{id}/send?sendTo=…`. The endpoint requires `Content-Type: application/octet-stream` on an empty POST body — handled by a new `qbo.send` helper in `qbo.apis.js` plus a `contentType` plumb-through in `rawRequest`. New lifecycle dispatcher `dispatchInvoiceLifecycleEmails({ invoice, customerMap, event })` in `services/invoice/invoice.service.js` decides when to send: (a) `event='created'` delivers the initial invoice email exactly once (guarded by `invoiceEmailSentAt`); (b) `event='payment'` re-sends the invoice email whenever a successful payment grew `amountPaid` (caught via `invoiceEmailedAmountPaid` snapshot) or transitioned `paymentStatus` (caught via `invoiceEmailedStatus` snapshot) since the last send. Every partial payment naturally triggers a re-send via the amount-grew check, so the customer's emailed copy reflects the new balance after each payment without needing a separate payment-receipt channel — QBO's `/send` always mails the CURRENT invoice document (with all recorded payments + updated balance). Wired into `createInvoiceForOrder` (Phase 4, before save) and `propagateSuccessfulPayment` (just before final save) so cheque receipts (`recordManualPayment`), NMI card charges (`chargeInvoice`), and CRON PASS 2 sync retries all route through the same path. New Invoice fields: `invoiceEmailSentAt`, `invoiceEmailLastSentAt`, `invoiceEmailedStatus`, `invoiceEmailedAmountPaid`, `lastEmailError`. Emails are best-effort — QBO `/send` failures land on `lastEmailError` and never throw upward, so a temporary mailer outage cannot block payment bookkeeping. The CRON sweep self-heals unsent emails — next propagate run sees `amountPaid > invoiceEmailedAmountPaid` (or a status mismatch) and re-sends. Scope note: an initial implementation also added per-payment receipt emails via `POST /payment/{id}/send` + a `paymentEmailedQboIds[]` Invoice field; both were removed when the requirement was narrowed to "invoice-only" — the invoice re-send carries every payment detail the customer needs. Spec: INTEGRATIONS.md §7.7 (new — customer email lifecycle; numbered §7.6 at the time of this change, renumbered when the QBO admin section landed on 2026-05-28).

- **2026-05-25** — Fixed Order List showing "Processing: Scheduled / Payment: In progress" after a successful card-fallback charge on a partially-paid invoice. Root cause: `deriveInvoicePaymentStatus` treated `'in_progress'` as sticky. `chargeInvoice` writes `paymentStatus='in_progress'` as a transient lock before the NMI sale and calls `applyDerivedPaymentStatus(invoice)` right after the charge to release it — but the sticky check returned `'in_progress'` unchanged, so the status never transitioned to `'paid'` even though `amountPaid` now equaled `amountDue`. Cascading effect: `propagateSuccessfulPayment`'s `if (invoice.paymentStatus === 'paid')` branch never fired, so `ShopifyOrder.processingStatus` stayed `'scheduled'` and `paidAt` / `completedAt` were never set. Fix: removed `'in_progress'` AND `'failed'` from the sticky set in `deriveInvoicePaymentStatus` (only `'cancelled'` remains sticky — the only state with admin / webhook intent that should resist amount-based derivation). `'in_progress'` is a transient lock managed by `chargeInvoice` itself; deriving after the charge IS the lock release. `'failed'` no longer being sticky means a manual cheque receipt landing on a previously-failed invoice correctly transitions to `partially_paid` / `paid` since the money actually arrived — CRON PASS 1's `paymentStatus: 'pending'` filter still prevents auto-charge retries. Recovery for existing stuck invoices: added a self-heal at the top of `propagateSuccessfulPayment` (re-derives status before any downstream work) and expanded CRON PASS 2's sweep cursor to include `'in_progress'`. The race between the sweep and a truly in-flight chargeInvoice is benign — propagate's diff-against-cumulative sync never posts duplicates, and chargeInvoice's own save() runs after the loop. Spec: INTEGRATIONS.md §11 (status derivation rules).

- **2026-05-25** — Fixed "Charge card on file" button incorrectly disabled when a vault exists. `customer_maps.nmiCustomerVaultId` is just a cache populated at order intake; the source of truth is `wholesale_applications.nmiCustomerVaultId`. If a customer captured a card after their order was processed (or for orders pre-dating the cumulative-sync customer.service refactor), the cache was empty while the source had the real vault id, so the Order Details page's `canChargeCard = !!customerMap?.nmiCustomerVaultId` gating disabled the button and `charge-card.js` / `retry-payment.js` returned 409. New helper `customer.service.resolveCustomerVaultId({ shop, email, customerMap })` — fast-paths the cache hit, falls through to wholesale_applications on miss, and lazily syncs the cache back so the next reader sees the resolved id. Wired into the Order Details loader (button-gating self-heals) + both admin charge endpoints (lazy-sync the in-memory map so `chargeInvoice`'s downstream read picks up the resolved id). New "No card on file" warning banner replaces the previous subdued one-liner so admins see immediately when this customer truly has no saved card vs. when the cache was just stale. Spec: INTEGRATIONS.md §6.

- **2026-05-25** — Fixed duplicate-QBO-payment bug caused by non-idempotent retries. Symptom: one local `recordManualPayment` call (one `PaymentAttempt` row, $15 cheque) but TWO `Payment` records on the QBO invoice (Balance shows $1 instead of $16 against a $31 invoice). Root cause: `qbo.apis.qboRequest` wraps `rawRequest` in a transient-error retry loop (default 4 attempts), and `propagateSuccessfulPayment` adds another `syncWithRetry` (3 attempts) on top. When QBO's POST `/payment` committed server-side but the response was slow / hit a 502 / dropped a TCP packet on the way back, the retry layer fired the POST a second time — QBO had no way to know it was a retry and cheerfully created a duplicate. Fix: pass QBO's documented `requestid` idempotency token on every POST. New helper generates one UUID per logical `qboRequest()` call (before the retry loop) and threads it through `rawRequest` and the 401-refresh recursion, so all internal retries share the same id and QBO dedups them server-side. Only mutating verbs (POST/PUT) get the id — GETs / queries are inherently idempotent. Callers can pin a specific id via `opts.requestId` for cross-process idempotency (e.g. resuming a crashed job). Spec: INTEGRATIONS.md §7 (QBO transport).
- **2026-05-25** — `orders/cancelled` webhook integrated. New route `routes/webhooks.orders.cancelled.jsx` and orchestrator `services/order/order.service.handleOrderCancelled` mirror the create-handler shape (HMAC verify, fire-and-forget, idempotent on `seenWebhookIds[]`). Effects: upserts the local `ShopifyOrder` to `processingStatus: 'cancelled'` (covers the case where the cancel webhook beats the create webhook); flips any linked `Invoice.paymentStatus → 'cancelled'`; voids the QBO invoice via new `qbo.service.voidInvoice` ONLY when `amountPaid === 0` (paid/partially-paid invoices are left intact so the admin can decide on a refund manually); appends a `system_note` remark to the invoice timeline. New `ShopifyOrder` fields: `cancelledAt`, `cancelReason`; new enum value `'cancelled'` on `processingStatus`. CRON skip is automatic — PASS 1's existing `paymentStatus: 'pending'` filter excludes `'cancelled'`. Race protection: `processShopifyOrder` re-fetches `cancelledAt` immediately after invoice creation and aborts (cancelling + voiding the just-created invoice) if the cancel webhook ran during processing — handles the narrow window where the cancel webhook flips the order while create is mid-flight and `local.save()` would otherwise overwrite `cancelled → invoiced`. Subscription registered both declaratively (in the dev-rk profile) and programmatically (in `shopify.constants.REQUIRED_SUBSCRIPTIONS` — covers the protected-customer-data approval gap on the default profile). UI: `ProcessingBadge` gains a `cancelled` tone (default/grey); Order List adds a "Cancelled" filter chip. Spec: INTEGRATIONS.md §4 + §5 (cancellation flow).

- **2026-05-25** — Removed refund + cancel-invoice functionality per scope change. Dropped: `POST /api/admin/orders/:id/refund` + `POST /api/admin/orders/:id/cancel-invoice` endpoints; `invoice.service.refundInvoice` + `cancelInvoice` orchestrators; `qbo.service.recordRefund` (RefundReceipt); `shopify.service.refundShopifyOrder` + `MUTATION_REFUND_CREATE` + `QUERY_ORDER_REFUNDABLE`; the `Invoice.refunds[]` ledger, `Invoice.amountRefunded`, `Invoice.cancelledAt/By/Reason` fields; the `partially_refunded` / `refunded` enum values from `Invoice.paymentStatus`; Refund + Cancel UI (buttons, modals, fetchers, ledger card, cancellation banner, Refunded/Refundable summary KVs) from Order Details; `partially_refunded` / `refunded` badge tones from `admin-ui.PaymentStatusBadge` + Order List `PaymentBadge`. `nmi.service.refundTransaction` reverted to its original simpler form (still exported as an admin-tooling helper but no caller wires it up). Partial-payment support (the `partially_paid` status + cumulative QBO/Shopify sync — `qboRecordedTotal`, `qboPaymentIds[]`, `shopifyRecordedTotal`, `shopifyTransactionIds[]`, `recordOrderTransaction` REST helper) is RETAINED — that was a separate requirement, not part of the refund/cancel scope being rolled back. Spec: INTEGRATIONS.md §9 (removed §9.6 + §9.7 + §9.8 — derivation lives in code now).

- **2026-05-25** — Multi-payment downstream sync — each partial payment now lands its own QBO `Payment` record and its own Shopify SALE transaction, instead of being skipped after the first one. Root cause of the prior bug: `propagateSuccessfulPayment` gated QBO's `recordPayment` on the single boolean `qboPaymentRecorded`, so the second cheque receipt's call short-circuited and QBO's invoice balance stayed at the partial. New Invoice fields: `qboRecordedTotal` + `qboPaymentIds[]` (cumulative QBO sync state) and `shopifyRecordedTotal` + `shopifyTransactionIds[]` (cumulative Shopify sync state). Each propagate call now records the DIFFERENCE between `amountPaid` and what's already been booked on each side. New Shopify path: `shopify.service.recordOrderTransaction` posts a manual SALE transaction via Admin REST (`/admin/api/{version}/orders/{id}/transactions.json`) for every partial — Shopify's `displayFinancialStatus` updates to `partially_paid` / `paid` as transactions accumulate. `orderMarkAsPaid` still fires once on full settlement (idempotent — Shopify returns "already paid" when transactions already cover the total) so downstream Shopify workflows still trigger. Backward-compat: legacy invoices with `qboPaymentRecorded: true` (or `shopifyMarkedPaid: true`) but no cumulative total backfill `qboRecordedTotal`/`shopifyRecordedTotal` from `amountPaid` on first propagate so they don't double-record. CRON PASS 2 cursor expanded with `$expr`-based cumulative-mismatch checks so any invoice (paid OR partially_paid OR partially_refunded) with unsynced amount gets re-swept. New low-level helper `shopify.apis.shopifyRestPost` (Shopify Admin GraphQL has no equivalent mutation for manual SALE recording on orders without an existing AUTHORIZATION). Spec: INTEGRATIONS.md §12 (rewritten — partial-payment sync).

- **2026-05-25** — Partial payments, refunds, and admin-cancel are first-class. `Invoice.paymentStatus` enum extended with `partially_paid`, `partially_refunded`, `refunded`; transitions go through a single derivation helper `deriveInvoicePaymentStatus` (`invoice.utils.js`) so no service ever sets the status ad-hoc. New schema: `Invoice.refunds[]` (per-refund kind/amount/NMI+QBO+Shopify sync flags), `Invoice.amountRefunded` (denormalized), `Invoice.cancelledAt/By/Reason`. New endpoints: `POST /api/admin/orders/:id/refund` (NMI refund against most-recent approved sale, falls back to manual ledger when no NMI sale exists; QBO `RefundReceipt`; Shopify `refundCreate`) and `POST /api/admin/orders/:id/cancel-invoice` (flips `paymentStatus → 'cancelled'`; CRON auto-skips via the existing `paymentStatus: 'pending'` filter). `retry-payment.js` now accepts an optional `amount` body for partial card charges; `chargeInvoice` clips against remaining outstanding and only stages the processing-fee line on the charge that actually settles. Order Details surfaces Refund + Cancel buttons (with eligibility flags `canRefund` / `canCancel`), a Refundable KV, a refunds ledger card, and a cancellation banner. Shopify `orderMarkAsPaid` only fires on full settlement now — partials sync the local mirror with `financialStatus: 'partially_paid'` and defer the Shopify mark-paid call. Spec: INTEGRATIONS.md §6, §9, §11 (refund flow + cancel flow), §17 (Invoice collection schema).

- **2026-05-25** — `ProcessingBadge` now swaps its label when an order's `processingStatus === 'scheduled'` but the linked invoice's `paymentMethod` is `check` or `ach`. Cheque invoices render **"Awaiting cheque"** (warning tone) and ACH invoices render **"Awaiting ACH"** — "Scheduled" implied CRON auto-charge, which is misleading since the scheduler intentionally skips non-card invoices (§9.2). Card invoices still render "Scheduled" as before. Implementation: optional `paymentMethod` prop on `components/admin-ui.ProcessingBadge`; passed from both the Order List (`app.orders._index.jsx`) and Order Details (`app.orders.$id.jsx`). No model / API / scheduler changes.

- **2026-05-25** — NMI Customer Vault is now created **exactly once per customer**, at wholesale-registration submit (`app/api/registration-form.js`). Every downstream flow reads through `wholesale_applications.nmiCustomerVaultId` rather than re-creating: `customer.service.ensureCustomerForOrder` mirrors the id onto `CustomerMap.nmiCustomerVaultId` and validates it via the new `validateCustomerVault(vaultId)` helper (`query.php?report_type=customer_vault&customer_vault_id=…`); `payment.service.chargeInvoice` re-runs the same pre-flight before every NMI sale and writes a `skipped` PaymentAttempt with `"NMI vault X invalid: …"` if the id no longer resolves (vault deleted out-of-band, env swap, etc.). Removed: `nmi.service.findOrCreateCustomerVault` call from `customer.service` (still exported for legacy/diagnostic use); the `paymentDetails.service.js` strategy registry + its dev-only static-test-card strategy (it only fed the now-dead vault-create path on the order side). `customer.service.ensureCustomerForOrder` no longer accepts a `paymentDetails` argument. Spec: INTEGRATIONS.md §6.3 (rewritten), §6.4 (new — vault sourcing), §8.3 (rewritten — three vault-facing helpers).

- **2026-05-22** — QBO invoice payload now includes `ShipAddr` and `ShipDate`. Address derived via `customer.utils.buildProfileFromShopifyOrder` (shipping → billing → customer default fallback) and projected through new `qbo.utils.toQboAddress`, which `BillAddr` on the customer payload now also uses. Ship date is `order.created_at` formatted to `YYYY-MM-DD` via new `invoice.utils.toYmd` (Shopify's `orders/create` fires pre-fulfillment, so the order date is the best ship-on marker). Both fields are omitted when no source data is available. Spec: INTEGRATIONS.md §7.3 + §18.3.
- **2026-05-22** — Card invoices now carry a "Credit Card Processing Fee – 3%" line, appended by `shopifyLinesToQboLines` on top of the products + shipping + tax subtotal. Rate is `INVOICE_CREDIT_CARD_FEE_RATE` (default `0.03`); set to `0` to disable. Cheque/ACH invoices never carry the fee, and the surcharge is locked at invoice creation — the cheque → card admin fallback does NOT retroactively add a fee line (existing QBO line items are immutable). New helper `readNumber` in `utils/env.utils.js`. Spec: INTEGRATIONS.md §7.3 + env table.
- **2026-05-22** — Processing-fee model reworked from creation-time / single-rate to **settlement-time / per-method**. Three rates: card=`INVOICE_FEE_RATE_CARD` (3% default), ACH=`INVOICE_FEE_RATE_ACH` (1% default), cheque=`INVOICE_FEE_RATE_CHECK` (0% default). The fee follows the actual settlement method, not the customer's preference — every settlement path (CRON auto-charge, admin retry, cheque → card fallback, manual ACH receipt) lands the correct fee. New: `qbo.service.appendInvoiceLines` (GET + sparse update of the Line array with fresh SyncToken), `invoice.utils.computeProcessingFee` / `buildProcessingFeeLine` / `findExistingProcessingFeeLine` / `processingFeeLabel`, Invoice model fields `processingFeeAmount/Rate/Method/AppliedAt`, and a read-only confirmation endpoint `POST /api/admin/orders/:id/preview-payment` ({ method } → `{ baseAmount, feeAmount, newTotal, ... }`) for admin "confirm before charge" flows. Fee is applied only on successful settlement: declined card attempts leave no fee line. Cheque-preferred customers paying by card via admin fallback now correctly get the 3%. Spec: INTEGRATIONS.md §7.3 (rewritten) + env table.
- **2026-05-22** — New `Invoice.remarks[]` ledger powers a **Remarks** column on the Order List page. Each CRON tick now writes one entry per touched invoice (PASS 1: card-charge outcome; new PASS 1.5: cheque/ACH reminder + failed-card follow-up) and every admin settlement action (retry, charge-card fallback, mark cheque/ACH paid) appends an `admin_action` entry tagged with the operator's email. Three new invoice-scoped filter chips on the list: **Overdue** (`qboDueDate < today` AND unpaid), **Pending cheque** (cheque/ACH unpaid), **Failed payments** (`paymentStatus: 'failed'`). The Remarks cell shows a red "Payment Due — $X · OVERDUE" header for unpaid cheque/ACH invoices, a "Payment Failed — N/M attempts" header for failed invoices, the latest remark message + date, and a "+N more" tail counter pointing to Order Details. New helper `invoice.service.appendInvoiceRemark`. No customer-facing notifications sent yet — log-only. Spec: INTEGRATIONS.md §11 + Collections table.
- **2026-05-22** — Added full-datetime `Invoice.dueAt` field + `INVOICE_TERMS_MINUTES` env testing knob. `dueAt = order.created_at + termsDays + termsMinutes`; set `INVOICE_TERMS_MINUTES=1` to make every new invoice flag as Overdue ~1 minute after creation (lets admins watch the Overdue indicator + cheque-reminder UI fire without waiting whole days). QBO's date-only `DueDate` still uses `termsDays` only — `termsMinutes` is local-only. New helper `invoice.utils.computeInvoiceDueAt`; the Order List Overdue filter + `RemarksCell` + `DueDateCell` now go through a shared `isOverdueByInvoice` predicate that prefers `dueAt` and falls back to `qboDueDate` for older invoices.


- **2026-05-21** — Cheque/ACH workflow: invoices carry `paymentMethod` (locked at creation from `CustomerMap.paymentMethod`, which is sourced from `wholesale_applications.payment.method`). CRON scheduler now skips non-card invoices. Order Details page exposes "Mark cheque paid" (records cheque ref + propagates to QBO + Shopify) and "Charge card on file" (per-invoice override) for cheque/ACH invoices. New audit outcome `manual_paid`. Spec details in [INTEGRATIONS.md §9](wholesale/INTEGRATIONS.md), gating in §11.1, admin endpoints in §11.4, edge cases §22.11–22.12.

> **Heads up:** this file contains unresolved git merge conflict markers (lines around 96/104/151 and 192/193/196). They predate the changes in this entry — flagging so they can be resolved separately. The §4 Implementation Status / §6 Maintenance protocol / §8 Changelog headings referenced elsewhere were lost in the conflict; the snapshot + changelog above are a temporary stand-in until the conflict is cleaned up.
